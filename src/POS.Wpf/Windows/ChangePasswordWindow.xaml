<Window x:Class="POS.Wpf.Windows.ChangePasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="POS — Change password"
        Height="360"
        Width="380"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="CurrentPasswordLabel" Grid.Row="0" Margin="0,0,0,4" />
        <PasswordBox x:Name="CurrentPasswordBox" Grid.Row="1" Margin="0,0,0,12" />
        <TextBlock x:Name="NewPasswordLabel" Grid.Row="2" Margin="0,0,0,4" />
        <PasswordBox x:Name="NewPasswordBox" Grid.Row="3" Margin="0,0,0,12" />
        <TextBlock x:Name="ConfirmPasswordLabel" Grid.Row="4" Margin="0,0,0,4" />
        <PasswordBox x:Name="ConfirmPasswordBox" Grid.Row="5" Margin="0,0,0,12" />
        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Button x:Name="CancelBtn" Width="90" Margin="0,0,8,0" Click="CancelBtn_OnClick" />
            <Button x:Name="SaveBtn" Width="90" IsDefault="True" Click="SaveBtn_OnClick" />
        </StackPanel>
    </Grid>
</Window>
