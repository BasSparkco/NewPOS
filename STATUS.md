# POS — Status

Track completed **stages** and **steps** against [ROADMAP.md](ROADMAP.md). Update this file when something is done.

**Legend:** `[ ]` not started · `[~]` in progress · `[x]` done

---

## Stage 0 — Foundation

| Step | Status |
|------|--------|
| 0.1 Git repo + `.gitignore` | [x] |
| 0.2 Solution + Core / Application / Infrastructure / WPF projects | [x] |
| 0.3 EF Core + SQLite + DbContext + migrations (incl. Stores) | [x] |
| 0.4 DI + logging in WPF host | [x] |
| 0.5 Coding standards / folder layout documented | [x] |
| 0.6 Test project for unit tests | [x] |

**Stage 0 complete:** [x]

---

## Stage 1 — MVP

| Step | Status |
|------|--------|
| 1.1 Identity (minimal or full roles — document choice) | [x] |
| 1.2 Store seed + StoreId on invoices/inventory | [x] |
| 1.3 Products & categories CRUD | [x] |
| 1.4 Inventory per store; stock on sale | [x] |
| 1.5 Invoice + line items + Open → Paid | [x] |
| 1.6 Cash payment + totals validation | [x] |
| 1.7 Main POS UI (search, lines, complete sale) | [x] |
| 1.8 ESC/POS receipt printing | [x] |
| 1.9 SQLite deployment / backup notes | [x] |

**Identity choice (1.1):** Minimal login — `Users` + `Roles` in SQLite with seeded demo users **`admin`** and **`cashier`**. Password hashes still exist in storage, but the current WPF login flow is username-only. Session is in-memory after sign-in (`ICurrentSession`).

**Stage 1 complete:** [x]

---

## Stage 2 — Operations & UX

| Step | Status | Notes |
|------|--------|-------|
| 2.1 Multi-invoice tabs + hold/resume | [x] | Tabs, concurrent invoices, Hold/Resume (F3), orange dot on held tabs, guard on pay |
| 2.2 Users & roles + RBAC | [x] | Real granular RBAC: `Role.PermissionsMask` (`[Flags] Permission`: ManageProducts/ViewReports/ViewAudit/ManageUsers/ManageSettings/ProcessRefunds) replaces the old binary `IsAdmin` gate in the WPF app; the "Users" screen (sidebar) edits a role's permissions directly, and permission edits sync between devices via the existing offline sync engine. Admin role always keeps ManageUsers (server-enforced) to prevent lockout. `POS.Web` dashboard gating and `POS.Api` JWT claims are unchanged (still coarse Admin/Manager-only) — tracked as the remaining follow-up in [ROADMAP.md](ROADMAP.md) |
| 2.3 Refund / cancel + stock movements | [x] | RefundInvoiceAsync restores stock; RefundWindow lists recent paid invoices; gated by the ProcessRefunds permission (previously undocumented as ungated — any signed-in user could open Refund until this pass) |
| 2.4 Quick price check + low-stock alerts | [x] | Price check window (F2), low-stock badge on cards + detail view |
| 2.5 Local reports (daily sales, basic metrics) | [x] | ReportsWindow: revenue, invoices, items sold, avg, top products, per-invoice list; date picker |
| 2.6 Customer display window | [x] | CustomerDisplayWindow: live cart + total, clock, idle screen; toggle via header button |
| 2.7 Barcode + printer hardening (real hardware) | [x] | Scanner burst detection (timing + Enter) tested; exact-barcode auto-add enabled; raw print verifies bytes written and safely falls back to file |
| 2.8 i18n + RTL (EN/AR/HE) | [x] | EN/AR/HE: main cashier (VM) + subsidiary windows via `Localization/AppStrings*.resx`, `Locale` helper, `CurrentUICulture`; RTL FlowDirection on dialogs; barcode row uses localized format |
| 2.9+2.10 UI Overhaul (Bagisto + pos_cashier.html) | [x] | Sidebar nav (Home/Customers/Cashier/Orders/Products/Reports/Settings), top bar with refresh(F5)/fullscreen(F11)/dark-mode/customer-display/price-check(F2), large Bagisto-style product cards with shadow hover, collapsible numpad, DynamicResource dark mode |

**Stage 2 complete:** [x]

---

## Stage 3 — Multi-currency & commercial depth

| Step | Status |
|------|--------|
| 3.1 Currencies + exchange rates + policy | [x] |
| 3.2 Discounts & taxes on invoices | [x] |
| 3.3 Optional: advanced pricing, barcodes, units | [ ] |
| 3.4 Stock movements audit + inventory snapshot | [x] |
| 3.5 Settings UI (general settings beyond currency policy) | [x] |
| 3.6 Audit logs | [x] |

**3.1 complete:** Currency entities/migration are in place, seeded currencies exist, stores carry a base currency, session/UI display the store currency, new sales inherit the store base currency, and admins now have a settings window to manage exchange rates and base-currency policy. Changing the base currency re-normalizes rates and converts catalog pricing plus open invoices into the new base currency.

**3.4 complete:** Stock changes are now written to a dedicated `StockMovements` ledger for opening stock, manual stock set adjustments, completed sales, and refunds. Admins can review the current inventory snapshot and recent movement history from the product management screen, including filtering history by the selected product.

**3.5 complete:** The admin settings window now includes persisted operational settings in addition to currency policy. Low-stock threshold, negative-stock policy, default tax percent, and receipt footer text are stored through the typed settings service and editable from the desktop app.

**3.6 complete:** A dedicated `AuditLogs` slice now records critical write-side actions, including currency policy changes, product create/update/delete, manual stock adjustments, sale completion, refunds, hold/resume, and cancel flows. Admins can review the audit trail from the desktop audit viewer with date, action, and entity filters.

**Stage 3 note:** Step 3.3 remains optional and should not block Stage 4 unless the next release explicitly needs advanced pricing, barcode variants, or units/weighted-product workflows.

**Stage 3 complete:** [x]

---

## Stage 4 — API & sync

| Step | Status |
|------|--------|
| 4.1 ASP.NET Core API + PostgreSQL | [x] |
| 4.2 JWT auth | [x] |
| 4.3 Device + sync fields on entities | [x] |
| 4.4 Sync engine + conflict strategy | [x] |
| 4.5 Deployment + migrations (local/server) | [x] |

**4.1 complete:** `POS.Api` now exists as an ASP.NET Core host, Swagger is enabled in development, infrastructure can target PostgreSQL or SQLite from configuration, and the solution includes the first API deployment surface alongside the desktop app.

**4.2 complete:** The API now issues JWT bearer tokens from the existing username-only login flow, hydrates session context from claims, and exposes authenticated catalog, store, and sales endpoints. Focused API integration tests cover login, catalog access, sale mutations, hold/resume/cancel, cash completion, and refund.

**4.3 complete:** The persistence model now includes `Device`, invoice `DeviceId`, invoice `SyncVersion`, and invoice `IsSynced`. The current host device is auto-registered per store, new invoices are stamped with that device and initial sync metadata, and invoice write paths increment `SyncVersion` while marking invoices unsynced whenever they change.

**4.4 complete:** The repo now includes outbound sync for invoices, audit logs, categories, products, devices, users, store settings, and the store currency-policy aggregate, plus inbound pull slices for those same aggregates and for audit logs. The desktop app background sync worker now pushes currency policy first, then settings, users, categories, products, devices, invoices, and append-only audit rows, before pulling newer currency policy, settings, users, categories, products, devices, invoices, and finally audit logs through `IInvoiceSyncService`; `POS.Api` now exposes `/api/sync/invoices/push`, `/api/sync/invoices/pull`, `/api/sync/audit-logs/push`, `/api/sync/audit-logs/pull`, `/api/sync/categories/push`, `/api/sync/categories/pull`, `/api/sync/products/push`, `/api/sync/products/pull`, `/api/sync/devices/push`, `/api/sync/devices/pull`, `/api/sync/users/push`, `/api/sync/users/pull`, `/api/sync/settings/push`, `/api/sync/settings/pull`, `/api/sync/currency-policy/push`, and `/api/sync/currency-policy/pull`; and the client persists separate per-store cursors for each aggregate while applying higher-version invoice snapshots plus category/product/settings/user/device/currency changes and appended audit rows locally. The current conflict rule remains invoice-level version precedence for invoice aggregates, while category/product/settings/user/device/currency-policy sync uses aggregate `UpdatedAt` precedence with skipped snapshots when the destination side is already as new or newer. User sync reconciles by `User.Id` or username and creates missing roles by role name when a pushed or pulled user references one that is not present locally. Audit logs now sync in both directions under append-only idempotence, ordered by `CreatedAt` plus `AuditLog.Id`. Device sync now uses `UpdatedAt` plus `DeviceId` cursors in both directions, and invoice sync reuses the same freshness guard so older invoice metadata cannot regress a newer device row. Currency policy now syncs as a single aggregate because base-currency changes also convert catalog pricing and open invoices, so it must run before category, product, and invoice synchronization. The broader aggregate-coverage pass is complete for the current desktop/admin mutation surface; `Role` still has no standalone management flow, and `Store` profile fields currently have no write path beyond the already-synced currency-policy/settings behavior. The full `POS.Tests` suite currently passes at 58/58, so the Stage 4 exit criteria are satisfied within the documented sync rules. See [docs/SYNC_STRATEGY.md](docs/SYNC_STRATEGY.md).

**4.5 complete:** Runtime provider switching is in place for SQLite desktop and PostgreSQL API hosts, and the design-time EF factory follows the same provider/connection resolution path instead of being hardcoded to SQLite. The API host hardens startup behavior for non-development environments: `Jwt:SigningKey` is required and must not be a development placeholder, `Database:ApplyMigrationsOnStartup` and `Database:SeedDemoDataOnStartup` default to disabled outside development, and the HTTP edge is now explicit through `Http:RequireHttps`, `Http:UseHsts`, and `ReverseProxy:*` forwarded-header settings. The repo includes [docs/API_DEPLOYMENT.md](docs/API_DEPLOYMENT.md) with explicit `dotnet ef database update` examples for WPF/SQLite versus API/PostgreSQL startup projects, environment-variable guidance for connection strings and JWT secrets, reverse-proxy trust configuration, and rollout sequencing for one migrator instance plus regular API replicas.

**Stage 4 complete:** [x]

---

## Stage 4T — Multi-tenant SaaS foundation

Detailed plan: [tenant.md](tenant.md). Production-readiness prerequisite for Stage 5.1 and Stage 5.2 (multi-branch reporting).

| Step | Status |
|------|--------|
| T0 Repository audit and migration map | [x] |
| T1 Tenant schema, store access, verified data migration | [~] |
| T2 Trusted identity and server authorization | [~] |
| T3 Tenant-safe WPF and offline profiles | [~] |
| T4 Scoped synchronization and stock reconciliation | [~] |
| T4.5 Cash session management (added 2026-09-21, required before T7) | [~] |
| T5 Tenant-aware administration and branch access | [~] |
| T6 Isolation and resilience release gate | [~] |
| T7 Recovery rehearsal and controlled pilot | [ ] |

**2026-09-21 planning note:** Confirmed the tenancy model in `tenant.md`: true multi-tenant SaaS (shared DB), `Store` remains the branch/location entity (no separate `Branch` table), and `Device` is promoted to be the formal "Box" (register/terminal) a cashier logs into rather than a pure sync-identity row. Every invoice is now keyed by tenant + branch (`StoreId`) + box (`DeviceId`) + user (`UserId`).

**T0 complete:** Full repository audit recorded in [docs/TENANT_T0_AUDIT.md](docs/TENANT_T0_AUDIT.md) — entity ownership inventory, pre-existing gaps (unauthenticated login, unenforced device identity, global role-name matching in sync), and the old-to-new ownership map. Confirmed the database is demonstrably single-business, so one bootstrap tenant unambiguously owns every existing row. Baseline before this work: `dotnet build` clean, `dotnet test` 77/77 passing.

**T1 in progress:** Added `Tenant`, `TenantCurrencyRate` (splits per-tenant exchange rates off the now-global-reference-only `Currency` row so one tenant's base-currency change can never corrupt another tenant's rates), and `UserStoreAccess` entities. Added `TenantId` to Store, User, Role, Product, Category, Device, Inventory, StockMovement, Setting, AuditLog, Invoice, and SyncChange, plus `User.NormalizedUsername` and tenant-scoped uniqueness on `(TenantId, NormalizedUsername)` and `(TenantId, Name)` for Roles. `Invoice.DeviceId` is now required at the application/entity level (every write path always populates it; a one-time migration backfill covers any legacy row that predates `DeviceId`). Single migration `AddTenantFoundation` implements the full expand+backfill+constrain in one pass (no separate phases needed) using `AddColumn` with a uniform bootstrap-tenant default instead of `AlterColumn`, since EF Core's SQLite provider does not support `AlterColumnOperation` at all. Every application service, sync push/pull handler (both `POS.Api/Program.cs` and the WPF-side `InvoiceSyncService.cs` mirror), and the `POS.Web` user-management postback now stamps `TenantId` correctly; full `POS.Tests` suite (77/77) passes against the real migrated SQLite schema via `Database.Migrate()`, not just `EnsureCreated`.

**Known T1 gaps carried forward:**
- Only validated against SQLite so far — the same migration has not yet been rehearsed against PostgreSQL (production provider). Postgres supports `AlterColumn` natively so should behave differently there; needs its own rehearsal before a Postgres rollout.
- `Invoices.DeviceId` and `AuditLogs.StoreId` keep their pre-migration nullability at the **database** level (SQLite can't `AlterColumn` an existing table at all) — enforced at the entity/application level instead. Revisit when a real table-rebuild or Postgres-only tightening is worth the risk.
- `POST /api/sync/categories/push` still has no auth context to derive a tenant from (pre-existing gap, documented in the T0 audit) — falls back to the sole bootstrap tenant; needs a real fix in T4.
- Read-side sync/query scoping by tenant (not just correct writes) is explicitly T4 scope, not done here.
- A hard-coded uppercase-GUID-literal gotcha was hit and fixed during T1: `Microsoft.Data.Sqlite` binds `Guid` parameters as uppercase text while `Guid.ToString()` is lowercase, so raw-SQL-seeded GUIDs (this migration's bootstrap tenant, and the pre-existing `Currencies.Id` seed data) must be uppercased or every future EF-written FK reference to them silently fails. Worth remembering for any future hand-authored migration in this codebase.

**T2 in progress — real password authentication landed (2026-09-21):** `AuthService.LoginAsync` previously matched on username only and never checked `PasswordHash` at all (T0 audit finding — this was a genuine unauthenticated-login gap, not just "weak auth"). Fixed:
- `IAuthService.LoginAsync(username, password, ...)` now verifies the password with `BCrypt.Net.BCrypt.Verify` against the stored hash; a bad username, inactive account, or wrong password all return the same generic "Invalid username or password." (no enumeration).
- WPF `LoginWindow` has a real `PasswordBox` now (was username-only); `POS.Api`'s `LoginRequest` and `POS.Web`'s `AccountController`/`Login.cshtml` both take a password.
- Seeded demo users (`admin`/`cashier`) now get randomly generated passwords instead of the trivial `"admin"`/`"cashier"` literals — see `DatabaseSeeder.DemoAdminPassword`/`DemoCashierPassword` and [start.md](start.md) for the actual values.
- New users created via the WPF Users screen (`UserManagementService.CreateUserAsync`) or the web dashboard's Management page (`ManagementController.CreateUser`) — both previously set `PasswordHash = string.Empty`, meaning no one could ever have logged in as a newly created user even before this pass — now get a random temporary password (`RandomPasswordGenerator`, `POS.Application/Support`) shown once at creation time (`Users_TemporaryPasswordFormat` message box in WPF; `TempData["ManagementSuccess"]` in Web). There is no forced-password-change-on-first-login flow yet.
- Full `POS.Tests` suite (77/77) passes with real password checks exercised end-to-end (API JWT login, Web cookie login, WPF `AuthService` unit-level paths).

**T2 in progress — device (Box) enrollment landed (2026-09-21, same day, follow-up turn):** T0 audit finding #5 ("Device identity is fully unauthenticated and auto-provisioned") fixed as an opt-in feature:
- `Device` gained `EnrollmentCodeHash`, `EnrolledAt`, `IsRevoked`, `RevokedAt` (migration `AddDeviceEnrollment` — plain `AddColumn`s only, no `AlterColumn`, so it's SQLite-safe by construction this time).
- New `IDeviceManagementService`/`DeviceManagementService`: `ProvisionDeviceAsync` (admin creates a not-yet-enrolled device row + one-time enrollment code, shown once — only its BCrypt hash is stored), `EnrollCurrentMachineAsync` (redeems the code and rebinds that device row's `Name` to the current machine, consuming the code), `RevokeDeviceAsync`, `GetDevicesAsync`.
- New WPF "Devices" screen (`DeviceManagementWindow`/`ViewModel`, sidebar entry gated on `Permission.ManageSettings`) to provision, revoke, and self-enroll from the same screen.
- `SaleService.GetOrCreateCurrentDeviceAsync` now throws `DeviceNotAuthorizedException` (caught and shown as a friendly warning in `MainViewModel.NewSaleAsync`) when: the matched device `IsRevoked` (**always enforced, unconditionally** — this part needed no opt-in), or when `Sync:RequireDeviceEnrollment=true` and the device is either missing entirely or provisioned-but-not-yet-enrolled. That config key is **not** set anywhere by default (absent = `false` everywhere, including `POS.Wpf/appsettings.json`), so the existing lenient auto-create-and-trust behavior is completely unchanged out of the box — a store opts into strict enrollment by adding the key to its own `appsettings.json`. This was deliberate: flipping the default now would have broken the demo quick-start flow in `start.md` (no device is pre-provisioned/enrolled for the seeded demo data).
- Enrollment state syncs like any other `Device` field (`DeviceSyncDto` extended, both `UpsertDeviceSnapshotAsync` copies updated) — but revocation is deliberately **sticky** in that sync path: an incoming snapshot can only ever flip `IsRevoked` from false→true, never back, so a stale/compromised client pushing an out-of-date "not revoked" snapshot can't un-revoke itself.
- 6 new tests (`DeviceManagementServiceTests`) cover provisioning, wrong-code rejection, successful enrollment + one-time-code consumption, sale blocked when enrollment is required and the machine isn't provisioned, sale succeeds after enrollment, and revocation blocking sales even in the default lenient mode. Full `POS.Tests` suite: 83/83 passing.

**Important gap surfaced by this work, relevant to the original "multiple Boxes per branch" request:** enrollment only helps a *second physical machine that already has a correctly-configured local database for the same store*. A genuinely fresh WPF install today has no bootstrap flow to "join an existing store" at all — `DatabaseSeeder` unconditionally seeds a **brand-new independent tenant** on first run if none exists locally, so a second real cash register can't yet just be installed and pointed at the same store; that bootstrap flow is T3 scope ("Tenant-safe WPF and offline profiles" — tenant.md explicitly calls out "a separate enrolled data profile/bootstrap" for this). Device enrollment as built here is the right foundation for that later flow, but does not by itself solve "add a second real register."

**Bug found and fixed the same day (2026-09-21):** the password-authentication pass above shipped `NormalizeBrokenDemoPasswordHashes`, a startup fixup that only re-hashes *broken* (empty/plaintext/malformed) demo-user password hashes — it deliberately left any *valid* BCrypt hash alone, including a hash for a different password, on the theory that a real user might have changed it. That safety check backfired for every pre-existing local dev database: `pos.db` files seeded before this feature existed (back in Stage 1) hold a **valid** BCrypt hash for the old literal password `"admin"`/`"cashier"`, so those installs silently kept rejecting the new documented demo passwords in [start.md](start.md) forever, with no way to self-heal short of deleting the database file. Fixed in `DatabaseSeeder.NormalizeBrokenDemoPasswordHashes`: the two seeded demo accounts now also get upgraded when their hash verifies against the specific pre-9/21 literal username-as-password value, while a hash matching neither the current nor that legacy literal password is still left untouched (so an intentionally-changed password is still never clobbered). Verified directly against the reporter's actual local `pos.db` (hash matched `"admin"`, not the new password) before and after the fix.

**T2 in progress — tenant-scoped login landed (2026-09-21, same day, follow-up turn):** `AuthService.LoginAsync` resolved its username lookup with no `TenantId` filter at all (`db.Users.FirstOrDefaultAsync(u => u.Username.ToLower() == nameLower ...)`), so the tenant.md T2 exit criterion "same username works independently in two tenants" did not actually hold — with two tenants both containing an `admin` user, login would silently authenticate against whichever tenant's row EF returned first, regardless of which tenant's password was supplied. Not yet exploitable in practice (every current deployment seeds exactly one bootstrap tenant and there is still no tenant-provisioning flow), but a live latent cross-tenant auth bug once T5 adds real tenant onboarding. Fixed:
- `IAuthService.LoginAsync` gained an optional `tenantSlug` parameter. An explicit slug must match an active, non-deleted `Tenant.NormalizedSlug`. With no slug, the service auto-resolves only when exactly one active tenant exists system-wide (true for every desktop SQLite profile and today's single-bootstrap-tenant server) — the moment a second tenant exists, an omitted slug fails closed instead of guessing. A suspended tenant can never authenticate, slug or not.
- The user lookup itself is now scoped by the resolved `TenantId`, so two tenants can safely share the same username with independent passwords.
- `ICurrentSession` gained a `TenantId` member; `Set(...)` now takes it as its first parameter. All three implementations (`CurrentSession` in WPF, `ApiCurrentSession`, `WebCurrentSession`) plus the `TestCurrentSession` test fake were updated. `POS.Api` issues a `tenant_id` JWT claim on login and rehydrates it into `ApiCurrentSession` per request; `POS.Web` does the same as a cookie claim through `WebClaimTypes.TenantId`.
- `POS.Api`'s `LoginRequest` and `POS.Web`'s `LoginViewModel`/`Login.cshtml` gained an optional `TenantSlug`/"Business" field ("leave blank for a single-business deployment"). WPF's `LoginWindow` was deliberately left unchanged — a local WPF install only ever holds one tenant by construction (T3 scope will formalize per-install tenant binding), so the existing username/password-only flow keeps working via auto-resolution.
- 5 new tests (`AuthServiceTests`) cover: same username authenticating independently in two tenants, the right password rejected against the wrong tenant's slug, an omitted slug failing once a second tenant exists, an unknown slug being rejected, and a suspended tenant being blocked. Full `POS.Tests` suite: 88/88 passing.

**T2 in progress — granular Web/API permission enforcement landed (2026-09-21, same day, follow-up turn):** `POS.Web` gated its entire dashboard on `RequireRole("Admin", "Manager")` — a hardcoded role *name* check totally decoupled from the `Permission` bitmask WPF already enforces (a custom role named e.g. "Store Ops" with real permissions couldn't reach the dashboard at all; a role literally named "Manager" got full access even with `PermissionsMask = None`, since no seeded role is even named "Manager"). `POS.Api`'s JWT claims never carried permissions at all — every authenticated user, including a Cashier, could hit `/api/sales/{id}/refund` with no `ProcessRefunds` check, unlike WPF. Fixed:
- `permissions` now flows as a real claim on both surfaces: `POS.Api` issues it on JWT login and rehydrates it into `ApiCurrentSession` per request; `POS.Web` issues it as a cookie claim via `WebClaimTypes.Permissions`. Both previously hardcoded this to `0` with a "not wired up yet" comment.
- `POS.Web`'s `DashboardAccess` policy is now "any permission granted" (`PermissionsMask != Permission.None`) instead of a role-name check, so dashboard reachability now follows the same model as WPF regardless of what a role is named. New per-permission policies (`WebAuthorizationPolicies.ManageProducts/ViewReports/ViewAudit/ManageUsers/ManageSettings/ProcessRefunds`) gate each `ManagementController` mutating action and all of `DashboardController` (reports = `ViewReports`) individually — e.g. `CreateUser`/`UpdateUser` require `ManageUsers`, product/category CRUD require `ManageProducts`, settings/currency-policy updates require `ManageSettings`. `AccountController` now rejects sign-in for a user with `PermissionsMask == None` instead of checking the role name.
- `Views/Management/Index.cshtml` hides (not just disables) the six mutating forms — store profile, operational settings, currency policy, create-user, create-category, create-product — behind the matching `Session.HasPermission(...)` check, showing a "Read-only — requires the ... permission" notice instead; the inline roster edit/delete forms (existing users/categories/products lists) are left visible since the server-side `[Authorize]` on their actions already blocks unauthorized submission via the existing `AccessDeniedPath` redirect. `ViewAudit` has a policy defined but nothing to gate yet — `POS.Web` has no audit-log viewer at all (WPF-only today).
- `POS.Api`: only the refund endpoint (`POST /api/sales/{id}/refund`) is gated behind the new `ProcessRefundsPolicy`, matching WPF's existing enforcement for the same action. Every other endpoint (catalog, cart mutation, sync) deliberately stays at the existing "any authenticated user" `RequireAuthorization()` default — broader per-endpoint API permission coverage is future work, not part of this pass.
- 3 new tests: `ApiIntegrationTests.Refund_endpoint_rejects_a_user_without_ProcessRefunds_permission`, `WebIntegrationTests.Sign_in_is_rejected_for_a_user_with_no_granted_permission`, `WebIntegrationTests.User_without_ManageUsers_permission_cannot_create_users` (also asserts the create-user form is hidden and the user is never persisted). Full `POS.Tests` suite: 91/91 passing.

**T2 in progress — change/reset password flow landed (2026-09-21, same day, follow-up turn):** until now the only way to set a password was `CreateUserAsync`'s one-time random temporary password shown at creation — there was no way for a user to change their own password, or for an admin to reset a forgotten one, ever again. Fixed:
- `IUserManagementService` gained `ChangeOwnPasswordAsync(currentPassword, newPassword)` — verifies the caller's current password, rejects a new password under 8 characters, available to any authenticated user for their own account only (no permission required) — and `ResetUserPasswordAsync(userId)` — generates a new random temporary password the same way user creation does, requires `ManageUsers`, scoped to the caller's own store.
- New shared `PasswordHasher` (`POS.Infrastructure/Services`) dedupes the BCrypt-verify-with-malformed-hash-safety logic that both `AuthService` and the new `UserManagementService` methods need; `AuthService.VerifyPassword` was removed in favor of it.
- **WPF**: a new top-bar padlock button (available to every signed-in user, no permission gate — a Cashier with a temporary password needs this regardless of dashboard access) opens `ChangePasswordWindow` for self-service change. The existing Users screen gained a "Reset password" button next to Save/New, showing the new temporary password the same way user creation already does. Both are fully localized (EN/AR/HE).
- **Web**: `AccountController.ChangePassword` (GET/POST, `[Authorize]` only — reachable by anyone who can sign in) gives self-service change from a "Change password" link in the header nav; `ManagementController.ResetUserPassword` (`ManageUsers`-gated) adds a "Reset password" button to each row of the user roster, showing the generated temporary password via the existing flash-message mechanism.
- Deliberately **not** built for `POS.Api`: nothing calls user-management through the API's HTTP surface today (WPF/Web talk to the shared application services in-process, not over HTTP), so there's no client yet for a `/api/auth/change-password` endpoint — add one if/when a real API consumer needs it.
- 5 new tests: `UserManagementServiceTests.ChangeOwnPassword_succeeds_with_correct_current_password_and_rejects_wrong_one`, `.ChangeOwnPassword_rejects_new_password_shorter_than_8_characters`, `.ResetUserPassword_generates_a_new_password_that_verifies_against_the_stored_hash`, `WebIntegrationTests.Self_service_change_password_updates_the_signed_in_users_own_credential`, `.Admin_can_reset_another_users_password`. Full `POS.Tests` suite: 96/96 passing.

**T2 in progress — minimal authenticated tenant/store/first-admin provisioning landed (2026-09-21, same day, follow-up turn):** until now there was no way to create a *second* tenant at all — `DatabaseSeeder` only ever creates one bootstrap tenant on first run, so T2's tenant-slug login had nothing real to resolve against, and the T2 exit criterion "same username works independently in two tenants" was only proven by test fixtures, never by an actual product flow. Fixed, deliberately staying "minimal" and explicitly *not* public self-service signup per tenant.md:
- New `ITenantProvisioningService`/`TenantProvisioningService` (`POS.Infrastructure`) creates a Tenant + its first Store + an Admin role with `Permission.All` + the first admin User + their `UserStoreAccess`, atomically. Validates a non-empty name/slug/store name/username, a unique slug, an admin password of at least 8 characters (matching `ChangeOwnPasswordAsync`'s rule), and that the requested base-currency code (defaults to `ILS`, matching `DatabaseSeeder`'s convention) exists as global reference data.
- Exposed only via `POS.Api`: `POST /api/platform/tenants`, disabled by default and only enabled by setting `Platform:ProvisioningSecret`; callers authenticate with that shared secret via an `X-Provisioning-Secret` header (constant-time compared), not a normal user/JWT identity — there's no user to log in as yet when bootstrapping a tenant's very first account. No UI anywhere calls this yet; it's operator-only (curl/ops script), matching "avoid public self-service signup in this milestone." Documented in [docs/API_DEPLOYMENT.md](docs/API_DEPLOYMENT.md).
- Config gotcha hit and fixed while building this: reading `Platform:ProvisioningSecret` from `builder.Configuration` *before* `builder.Build()` silently misses test-host configuration overrides (`WebApplicationFactory`'s injected config isn't guaranteed merged in until `Build()` runs) — moved the read to `app.Configuration` after `Build()`. `Jwt:SigningKey` happened to read fine at the earlier point only because `appsettings.Development.json` already supplies a fallback value that masked the same underlying timing issue; worth remembering for any future config read that must observe test-injected values.
- 5 new tests: `TenantProvisioningServiceTests` (creates tenant/store/admin, rejects a slug already in use, rejects a short password) plus `ApiIntegrationTests.Platform_tenant_provisioning_is_disabled_without_a_configured_secret` and `.Platform_tenant_provisioning_requires_the_configured_secret_and_creates_a_working_tenant` (proves the full loop: wrong/missing secret rejected, tenant created, its admin logs in with the new tenant's slug, and the *original* bootstrap tenant's `admin` still logs in independently with its own slug — the actual end-to-end proof of the T2 exit criterion). Full `POS.Tests` suite: 101/101 passing.

**Bug found and fixed the same day (2026-09-21, follow-up turn):** `InvoiceSyncService.TryCreateAuthorizedClientAsync` (the WPF background sync worker's own re-login to the API) sent `new { Username = _session.Username }` with no `Password` field. Once `/api/auth/login` started verifying a real password (the T2 password-auth pass earlier the same day), this call always failed `string.IsNullOrEmpty(password)` on the server, so **WPF-to-API sync silently stopped working entirely** for every real (non-mocked) deployment — every push/pull degraded to its "client is null" failure path without throwing, and no existing test caught it because every sync test fakes the `/api/auth/login` HTTP handler rather than exercising the real `AuthService`. Fixed:
- `ICurrentSession` gained `Password` (get) and `SetPassword(string?)`, kept in memory only (never persisted/logged). `AuthService.LoginAsync` calls `_session.SetPassword(password)` right after `_session.Set(...)`, so the verified plaintext password is available for the device's own outbound HTTP session for as long as that session stays signed in.
- `TryCreateAuthorizedClientAsync` now sends `{ Username, Password }` from the session and fails closed (returns `null` immediately, no HTTP call) if `Password` is empty.
- `UserManagementService.ChangeOwnPasswordAsync` also calls `_session.SetPassword(newPassword)` so a self-service password change doesn't leave the sync worker retrying a now-stale cached password until next login.
- 2 new tests (`InvoiceSyncServiceTests`) assert the real behavior: the login call now carries the session's password, and sync fails safe (no login attempt, invoices counted as failed, no crash) when `Password` is unset. Both were verified to fail against the pre-fix code before the fix was reapplied.

**T2 in progress — broader `POS.Api` sync-endpoint permission coverage landed (2026-09-21, same day, follow-up turn):** the sync push endpoints re-authenticate as the currently signed-in user's own verified identity (see the bug fix above), so — before this pass — **any authenticated user, including a Cashier with `Permission.None`, could push an arbitrary `Role.PermissionsMask`, product price, category, store setting, device row, or currency policy to the server** through `/api/sync/*/push`, with no permission check at all. This is exactly the "remove the ability to escalate privileges through existing user/role sync" gap tenant.md's T2 milestone calls out. Fixed:
- Three new authorization policies (`ManageUsersPolicy`, `ManageSettingsPolicy`, `ManageProductsPolicy`), matching the existing `ProcessRefundsPolicy` pattern (bitmask `HasFlag` assertion against the JWT's `permissions` claim).
- Gated to mirror the equivalent WPF/Web-side gate on the same mutation: `/api/sync/users/push` → `ManageUsers`; `/api/sync/settings/push` and `/api/sync/currency-policy/push` and `/api/sync/devices/push` → `ManageSettings`; `/api/sync/products/push` and `/api/sync/categories/push` → `ManageProducts`.
- Catalog browsing, cart/sale lifecycle endpoints, and every `/pull` endpoint deliberately stay at the existing "any authenticated user" default — matching WPF, where any signed-in cashier can sell and read-scoped sync data isn't a write-escalation risk. `InvoiceSyncService`'s push handlers already treat a non-success response as a soft per-aggregate failure (not a crash), so a lower-permission session simply skips pushing that aggregate on its next sync cycle rather than breaking sync overall.
- 1 new test (`Sync_push_endpoints_reject_a_user_without_the_matching_management_permission`) proves a Cashier-permission session gets `403 Forbidden` from all six gated push endpoints; verified to fail against the pre-fix code before the fix was reapplied. Full `POS.Tests` suite: 104/104 passing.

**T2 in progress — login rate limiting landed (2026-09-21, same day, follow-up turn):** tenant.md's identity section calls for rate limiting to slow brute-force/enumeration against login; neither `POS.Api` nor `POS.Web` had any. Fixed:
- Both hosts register `AddRateLimiter` with a fixed-window policy (10 requests/minute, partitioned by client IP, `429 Too Many Requests` on rejection) and apply it only to the login POST — `POS.Api`'s `POST /api/auth/login` via `.RequireRateLimiting("login")`, `POS.Web`'s `AccountController.Login` POST via `[EnableRateLimiting(WebRateLimitPolicies.Login)]`. Every other endpoint is unaffected.
- Doing this surfaced a real pre-existing inefficiency that would have made the limit unworkable: `InvoiceSyncBackgroundService`'s single sync pass calls up to 16 `IInvoiceSyncService` push/pull methods, and every one of them independently called `TryCreateAuthorizedClientAsync`, which re-logged in from scratch each time — 16 `/api/auth/login` calls per pass (every 30s by default). Fixed by caching the authorized `HttpClient` on the `InvoiceSyncService` instance (`_cachedAuthorizedClient`) — since it's `Scoped` and the background service creates a fresh scope per pass, this reduces it to exactly one login per sync pass without changing the per-pass scope of a fresh instance.
- 2 new tests (`Login_endpoint_rate_limits_repeated_attempts_from_the_same_client` in both `ApiIntegrationTests` and `WebIntegrationTests`) prove the 11th attempt within a minute gets `429`; both verified to fail (return `401`/generic error, not `429`) against the pre-fix code. Full `POS.Tests` suite: 106/106 passing.

**T2 in progress — `POS.Web` audit-log viewer landed (2026-09-21, same day, follow-up turn):** `ViewAudit` had a policy defined since the earlier granular-permissions pass but nothing to gate — `POS.Web` had no audit-log page at all (WPF-only). Fixed:
- New `AuditController` (`[Authorize(Policy = WebAuthorizationPolicies.ViewAudit)]` at the class level, matching `DashboardController`'s single-policy-per-controller pattern), reusing the existing store-scoped `IAuditLogService.GetRecentAsync` — no new query logic. Filters: from/to date, action (exact match), entity name (exact match).
- New `Views/Audit/Index.cshtml` and `AuditViewModel`/`AuditEntryRowViewModel`, styled consistently with the existing stock-ledger table (new `.table-row--audit` CSS rule alongside the existing `--inventory`/`--users`/`--invoices`/`--stock` variants). Nav link added to `_Layout.cshtml`.
- Caught and fixed a real bug before it shipped: the controller's query-string parameter was initially named `action`, which collides with ASP.NET Core's ambient route value `action` (the MVC action name, `"Index"`) — every filtered/unfiltered request silently bound to `"Index"` instead of the query string or `null`, so the page always rendered zero rows regardless of filters. Renamed to `actionName`. Caught by a test asserting on actual rendered row count (`table-row--audit` matches), not just substring presence — a weaker assertion (checking for the word "ProductUpdated" anywhere on the page) would have passed by accident because that string also appears in the filter input's placeholder text.
- 2 new tests (`Audit_page_lists_matching_entries_and_respects_filters`, `Audit_page_is_denied_without_ViewAudit_permission`). Full `POS.Tests` suite: 108/108 passing.

**T2 in progress — offline authorization expiry window landed (2026-09-21, same day, follow-up turn):** tenant.md §5 requires "a finite offline authorization validity period... do not silently permit indefinite cached administrative access." Until now a WPF device that stayed running (no restart) for weeks with no server contact kept full administrative access forever, with no way for a server-side revocation to ever reach it. Fixed, deliberately scoped to **administrative actions only** — selling stays available offline indefinitely, matching this POS's offline-first design; a chosen policy, not an oversight:
- `ICurrentSession` gained `LastOnlineContactUtc` (get) and `SetLastOnlineContactUtc(...)`, mirroring the `Password`/`SetPassword` pattern. `InvoiceSyncService.TryCreateAuthorizedClientAsync` records `DateTime.UtcNow` into it — and into a persisted `Sync.LastOnlineContactUtc` store setting, so it survives app restarts — on every successful online login (a real server-verified login is the strongest signal available that this device's user is still active and its credentials still valid). `AuthService.LoginAsync` reads that persisted value back into the session at every local login.
- New `IOfflineAuthorizationPolicy.IsAdministrativeAccessLocked()` (`POS.Infrastructure`): false whenever `Sync:Enabled` is false (no online concept for a deployment with no API at all — matches the demo quick-start, which ships with sync disabled by default); true if sync is enabled and this device has never once proven itself online; otherwise true once more than `Sync:OfflineAuthorizationHours` (default 24) have passed since `LastOnlineContactUtc`.
- `MainViewModel.EnsurePermission` (WPF's single choke point for `ManageProducts`/`ManageUsers`/`ManageSettings`/`ProcessRefunds` — already gates every admin screen/action) now also denies those four specifically when `IsAdministrativeAccessLocked()` is true, with a distinct "reconnect online first" status message (EN/AR/HE). `ViewReports`/`ViewAudit` (read-only) are deliberately not gated.
- 6 new tests: `OfflineAuthorizationPolicyTests` (sync disabled never locks; never-contacted locks; within/exceeding the default 24h window; configurable window) and `InvoiceSyncServiceTests.Push_unsynced_invoices_records_last_online_contact_after_a_successful_login` (proves a real sync login writes both the in-memory session value and the persisted setting). The window-detection assertions were verified to fail against a deliberately broken policy body before being confirmed correct. Full `POS.Tests` suite: 114/114 passing.

**Remaining T2 scope (not done in this pass):** an audit trail and any in-app UI for tenant provisioning (today it's silent and operator-only), and a per-request device credential for the sync protocol itself (today's enrollment code is a one-time setup gate, not a per-call credential — sync endpoints authenticate as the signed-in user, not the device, so a compromised device still impersonates whoever is currently logged in on it).

**T3 in progress — "join an existing business" bootstrap landed (2026-09-21, follow-up turn):** the real gap surfaced during T2's device-enrollment work: a genuinely fresh WPF install had no way to bind itself to an existing tenant/store — `DatabaseSeeder.SeedIfNeeded` always seeded a brand-new independent business, so a second real cash register could not simply be installed and pointed at an existing store. Fixed:
- New `IBusinessJoinService`/`BusinessJoinService` (`POS.Infrastructure`): given an API URL, business slug, username and password, logs into the remote server (`/api/auth/login`, reused as-is — confirmed with the user that this needed no new admin-issued-code API surface), decodes `tenant_id`/`permissions` from the returned JWT, fetches the store profile (`/api/stores/current`, also reused as-is), writes a minimal local `Tenant`+`Store`, populates `ICurrentSession` in memory, then reuses the existing, already-tested `IInvoiceSyncService` pull methods (`PullCurrencyPolicyAsync` → `PullRemoteSettingsAsync` → `PullRemoteUsersAsync` → `PullRemoteCategoriesAsync` → `PullRemoteProductsAsync` → `PullRemoteDevicesAsync`) to bootstrap everything else — no new reconciliation logic needed, since those methods already handle "the local row doesn't exist yet" as their ordinary steady-state case. Finishes with a real local `AuthService.LoginAsync` against the just-pulled user.
- **Real discovery made during this work, not assumed:** every database's migrations (`AddTenantFoundation`) unconditionally insert one placeholder "bootstrap tenant" row (plus one `TenantCurrencyRate` per global currency) into *every* database, including a brand-new one — to backfill any pre-existing single-tenant installs. This means `Tenants.Any()` is never a valid "is this fresh?" signal; `Users.Any()` is. `BusinessJoinService` defensively re-checks this itself (refuses to run if any local `User` already exists) before replacing the placeholder tenant/rate rows with the real remote ones.
- `InvoiceSyncService`'s pull methods each independently read `Sync:Enabled`/`Sync:ApiBaseUrl` from `IConfiguration` — `BusinessJoinService` sets both via `IConfiguration`'s indexer setter (takes effect immediately, in-process, no file or reload needed) before invoking them, using the server the operator just typed rather than whatever was previously configured.
- New WPF `SetupWindow`/`SetupViewModel`, shown once before `LoginWindow` only when the local database has no `User` rows yet, offering "start a new business" (today's unchanged `DatabaseSeeder.SeedIfNeeded` path) or "join an existing business" (the new flow above); on a successful join, `SetupViewModel` persists `Sync:Enabled`/`Sync:ApiBaseUrl` to a new `appsettings.local.json` (loaded via an additional `AddJsonFile(..., optional: true, reloadOnChange: true)` in `App.xaml.cs`) so future launches keep syncing, and `App.xaml.cs` skips `LoginWindow` entirely since the join already signed the session in for real. `App.xaml.cs`'s migration call changed from auto-seeding to `ApplyPosDatabaseMigrations(seedDemoData: false)` — every existing, already-configured install is unaffected (falls straight through to `LoginWindow` exactly as before).
- 3 new tests (`BusinessJoinServiceTests`): successful join (asserts local Tenant/Store/Users/Roles/Categories/Products/Inventory match the remote business and local login now succeeds), wrong password, and an unreachable server — both failure cases leave the local database still fresh (no `User` rows) so a retry is possible. A new `TestServiceHost.CreateAsync(..., seedBaseline: false)` option produces a genuinely empty, really-migrated local database (not `EnsureCreated`, to get the same fixed global-Currency IDs a real remote server's migrations produce) for this precondition. Full `POS.Tests` suite: 117/117 passing.
- **Deferred, not done in this pass:** re-pointing an *already provisioned* device at a different tenant (tenant.md calls this "profile maintenance" — requires a fresh bootstrap, not a relabel; no existing local profile to preserve here since it only applies to a fresh install); pulling historical invoices/audit logs during the join itself (the ordinary background sync loop, which starts every pull cursor at zero, backfills those automatically once the device starts running normally); WPF UI test coverage (this repo has none for any WPF screen — verification is via `BusinessJoinServiceTests` plus a documented manual walkthrough in `start.md`).

**Reconciliation pass (2026-09-21, new session) — user identity vs. device credential, Register vs. Device, cash sessions promoted to a required milestone:** the project owner asked for four requirements to be reconciled against actual code before continuing T2+: (1) device credentials must authenticate the terminal for background sync only, never replace employee login, and the server must validate authorization rather than trust a submitted UserId; (2) the logical register must be separable from the physical device so it survives a hardware swap; (3) a financial `CashSession` must be introduced as a required milestone, and employee logout must never close it; (4) configuration (not new code) should support both simple and larger-store cash workflows. A code audit (not a re-read of the docs) confirmed real gaps behind all four: `/api/sync/invoices/push` resolved the invoice's actor from a payload username string with a silent fallback to whoever was making the HTTP call on lookup failure (a real misattribution path); there was no device-scoped credential of any kind — sync always authenticated as the currently signed-in user; `Device` conflated the physical machine and the logical till into one row; there was no cash-session concept anywhere in the codebase; and WPF's sidebar "Logout" button was wired to `GoHomeCommand` — it never actually logged anyone out (just navigated to the cashier tab), so "logout must not close the cash session" had nothing to survive yet.

`tenant.md` and `ROADMAP.md` were updated first to encode the reconciled design (tenant.md §2.3a "Register vs. Device", §5a "Device authentication is not a substitute for employee login", §5b "Cash session management", plus a new required **T4.5** milestone inserted between T4 and T5, additional T6 test-matrix rows, and an updated §10 scope boundary explicitly excluding floating/shared-till workflows and full accounting-engine development). `ROADMAP.md`'s Promoted Extensions list marks "cash register management" as promoted out of the backlog rather than removing the historical entry. Implementation then proceeded against that reconciled design:

- **Schema/relationships established (T1/T4.5 scope, landed together deliberately — "establish the necessary relationships early"):** new `Register` entity (`Id, TenantId, StoreId, Number, Name, IsActive` — the stable till identity) and new `CashSession`/`CashMovement` entities (opening/closing cash, computed expected balance and discrepancy, per-movement `Method`/`CurrencyCode`, `InvoiceId`/`PaymentId` back-references, `PerformedByUserId`/`ApprovedByUserId`). `Device` gained `RegisterId` (which physical machine currently serves this till) and `DeviceSecretHash` (persistent sync credential, distinct from the one-time `EnrollmentCodeHash`). `Invoice` gained `RegisterId` alongside the existing `DeviceId`. `Payment` gained a nullable `CashSessionId`. One migration (`AddRegistersAndCashSessions`) — purely additive (`AddColumn`/`CreateTable`/`CreateIndex`/`AddForeignKey`, no `AlterColumn`, so it is SQLite-safe by the same rule as every migration since `AddTenantFoundation`).
- **A real, non-obvious tool gotcha hit and fixed while generating this migration:** the globally installed `dotnet-ef` CLI was v10.0.6 while the project's actual `Microsoft.EntityFrameworkCore` package is 8.0.11 — that version mismatch alone made `dotnet ef migrations add` scaffold a bogus ~340-operation, all-`AlterColumn` diff of the *entire* existing schema (and, separately, generating it against `POS.Api` as the startup project produced Postgres-typed columns, because `POS.Api`'s own `appsettings.json` defaults `Database:Provider` to `Postgres` — mixing that into a migration history whose existing files are all SQLite-typed would have broken every SQLite deployment). Fixed by pinning the global tool to `dotnet-ef --version 8.0.11` (matching the project) and forcing `Database__Provider=Sqlite` via an environment variable override when scaffolding against `POS.Api`. Worth remembering for any future hand-run `dotnet ef migrations add` in this repo.
- **Register backfill for pre-existing installs:** a new `RegisterBackfill.EnsureBackfilled(db)` (called from the same `ServiceProviderExtensions.ApplyPosDatabaseMigrations` choke point every host already calls after `Database.Migrate()`) creates one `Register` per pre-existing `Device` that lacks one, and backfills `Invoice.RegisterId` from each invoice's `Device`. Register ids are **deterministically derived from the Device's own id** (SHA-256-based, not `Guid.NewGuid()`) specifically so that a WPF install and its API server — which may each run this backfill independently against their own copy of the same already-Device-synced table — converge on the *same* Register identity without a coordinated migration run or a new sync round-trip; this is a deliberate, narrower substitute for giving `Register` its own push/pull sync surface (still open work). The invoice backfill uses a new `PosDbContext.SaveChangesWithoutSyncCapture()` escape hatch so retroactively stamping historical invoices with a `RegisterId` does not flood the sync engine with thousands of spurious "invoice changed" events on every existing installation's first post-upgrade startup. New devices provisioned from now on (`DeviceManagementService.ProvisionDeviceAsync`) get their own `Register` immediately, or — new parameter `replaceRegisterId` — bind to an *existing* register when replacing a till's hardware, refusing if that register still has another active (non-revoked) device attached.
- **Device authentication (tenant.md §5a):** `DeviceManagementService.EnrollCurrentMachineAsync` now also generates a persistent device secret (BCrypt-hashed into `DeviceSecretHash`, returned once like the existing one-time enrollment code). New `POST /api/devices/token` (deviceId + secret → a short-lived JWT carrying `tenant_id`/`store_id`/`device_id`/`token_type=device`, default 24h expiry via `Jwt:DeviceTokenMinutes`) lets a terminal authenticate itself without any employee credential. A new `token_type` claim distinguishes device tokens from user tokens; the API's authorization `DefaultPolicy` now rejects any device token outright (protects `/api/sales/*` and every endpoint using the bare `RequireAuthorization()` default), and a new `SyncPolicy` opts exactly nine `/api/sync/*` endpoints back in for device-or-user access: invoice push/pull, audit-log push/pull, and the categories/products/settings/users/devices read-only pulls. Administrative pushes (products, categories, settings, users, devices, currency policy) and `currency-policy/pull` (which internally reads `ICurrentSession.StoreId` via `ICurrencyService`, not verified safe for a device-only context that never populates that session) deliberately stay user-only.
  - **Real bug hit and fixed while wiring this:** ASP.NET Core *combines* (ANDs) a route-group's own `RequireAuthorization()` with each endpoint's own — so the `syncApi` group's pre-existing bare `.RequireAuthorization()` call silently re-imposed the new device-rejecting `DefaultPolicy` underneath every endpoint's `SyncPolicy`, defeating device-token sync entirely (every sync call returned 403 regardless of the endpoint-level policy). Fixed by removing the group-level call and giving every one of the 16 `/api/sync/*` endpoints its own explicit policy (`SyncPolicy`, a `ManageXPolicy`, or bare `RequireAuthorization()` for `currency-policy/pull`). Caught by an integration test that actually asserted the pull succeeded, not just that the sale attempt failed.
- **The actual "server must validate authorization rather than trust a submitted UserId" fix:** `/api/sync/invoices/push` previously resolved a pushed invoice's actor by looking up `incoming.Username` and, if that lookup failed for *any* reason (deleted user, renamed user, cross-tenant string, client bug), silently fell back to whichever identity was making the HTTP call — misattributing that historical sale to the wrong person. Fixed: the payload's username must resolve to a genuine, active user of the *same tenant* (not just the same store, tightened further) or the invoice is rejected outright (`"Failed"`, not applied) — it is never silently reattributed. A device-authenticated push additionally cannot claim a `DeviceId` other than the one it authenticated as. The pre-existing "no username at all → use the caller's own id" fallback is preserved for older/degenerate payloads, since that is not a misattribution (there was never a competing claim to override).
- **Cash session management (tenant.md §5b, T4.5):** new `ICashSessionService`/`CashSessionService` — `OpenSessionAsync`, `CloseSessionAsync` (computes `ExpectedCashAmount` from opening float + cash-method movements only, and `DiscrepancyAmount` against the counted amount), `RecordManualMovementAsync` (CashIn/CashOut; CashOut can require a second, differently-permissioned approver via a `Cash:RequireApprovalForCashOut` setting, off by default), and `RecordSalePaymentAsync`/`RecordRefundPaymentAsync` (best-effort, invoked from `SaleService.CompleteCashSaleAsync`/`RefundInvoiceAsync` — never blocks the sale/refund itself if no session happens to be open on that register). A `Cash:SessionMode` store setting selects `PerCashier` (default) vs `PerRegister` (shared); either way a DB-level filtered unique index (`CashSessions(RegisterId) WHERE Status = Open`) enforces at most one *open* session per register — the fixed-register model tenant.md's scope boundary calls for, not a floating till. A refund's `CashMovement` is tagged to whichever session is open *when the refund happens* (which may differ from the original sale's session) while its `InvoiceId`/`PaymentId` still point back to the original sale — preserving that connection without rewriting the original payment's own `CashSessionId`.
- **A real WPF logout, built because "logout must not close the cash session" presupposes a logout exists:** the sidebar "Logout" button was silently bound to `GoHomeCommand` (confirmed by direct code inspection, not assumed from the label). `MainViewModel` now has a real `LogoutCommand`/`LogoutRequested` event; `App.xaml.cs` handles it by closing the current `MainWindow`, clearing `ICurrentSession` (never touching any `CashSession` — that lives in the database keyed by Register/user, not in the in-memory session, so it survives by construction), and reopening `LoginWindow` for the next employee, using `ShutdownMode.OnExplicitShutdown` throughout so a plain window close (not a logout) still exits the app as before.
- **PostgreSQL migration status — a partial, honest answer, not a full rehearsal:** no Docker/`psql` is available in this environment, so a real running-Postgres rehearsal (the actual open item from T1) still has not happened and is not claimed here. What *was* newly verified: `dotnet ef migrations script` against the **entire** migration history (not just the new one), forced to the Npgsql provider, completes without error and renders syntactically plausible PostgreSQL DDL for the new tables (including the filtered unique index as `CREATE UNIQUE INDEX ... WHERE "Status" = 0`, valid Postgres partial-index syntax). This is strictly weaker evidence than an applied migration against a live database (it cannot catch extension/permission/runtime issues) but is stronger than the previous state, where this had never been checked at all for *any* migration in the history, not only the new one.
- **Tests:** 8 new tests — `ApiIntegrationTests.Device_token_authenticates_sync_but_is_rejected_from_business_operations` (issues a token, proves it can pull invoices, proves it is `403` from `/api/sales/open` and from an administrative sync push, proves a revoked device's token request is `401`) and `.Invoice_push_rejects_an_invoice_attributed_to_a_nonexistent_username_instead_of_misattributing_it` (proves the invoice is rejected rather than silently created under the caller's identity); `CashSessionServiceTests` (4 tests: opening records the float and blocks a second concurrent open session; closing computes expected balance/discrepancy from cash movements only and rejects re-closing; a cash sale followed by a same-session refund nets to zero and never blocks the sale even when no session was open yet; CashOut approval rejects self-approval and an unauthorized approver but accepts a real manager); `RegisterBackfillTests` (2 tests: a pre-existing device/invoice gets backfilled correctly and the backfill is idempotent; the id-derivation function is proven deterministic for the same input, the actual property the cross-node convergence design depends on). Full `POS.Tests` suite: **125/125 passing** (117 baseline + 8 new).
- **Deliberately not done in this pass, tracked as concrete follow-ups, not silently dropped:**
  - `Register` still has no push/pull sync surface of its own (relies on the deterministic-backfill convergence described above). A future admin-driven register rename/renumbering has nowhere to go yet.
  - `categories/pull` has no tenant/store scoping at all (confirmed by reading it, not assumed) — a pre-existing gap already flagged as T4 scope in the T0 audit, now additionally reachable via a device token since it was one of the nine endpoints granted `SyncPolicy`. Not new exposure (any authenticated user could already read it store-agnostically), but worth fixing in the same T4 pass that addresses read-side sync scoping generally.
  - T4's broader replay-safety audit (idempotency keys, tombstones, the full Section 6 contract) is unstarted beyond the invoice-push/pull actor-attribution fixes above. T5/T6/T7 are unstarted.

**T4/T4.5 follow-up (2026-09-21, new session) — cash-movement atomicity, close-session validation, actor-attribution parity, the device-ID reassignment fix, device-credential sync after logout, and the WPF cash-session UI:** the project owner asked for four things to be reconciled against real code and finished: (1) verify "cash recording never blocks the sale" without silently losing a required cash movement, with atomic local recording and replay-safe sync, and validated actor authorization; (2) prevent closing a cash session with incomplete financial records; (3) complete device-based synchronization after logout and fix the device-ID reassignment issue; (4) finish the cash-session UI and remaining validation. All four were implemented against the actual code (not assumed from prior notes) and are covered by new, verified-failing-before-fix tests:

- **Cash-movement recording is now atomic, not best-effort:** `CashSessionService.RecordSalePaymentAsync`/`RecordRefundPaymentAsync` used to open their *own* `DbContext` after `SaleService.CompleteCashSaleAsync`/`RefundInvoiceAsync` had already committed, wrapped in a blanket `try/catch` that swallowed every exception — including genuine local write failures, not just "no session is open." That meant a completed cash sale could silently end up with no cash-drawer ledger entry at all, with no error and no way to detect it later. Fixed: a new internal (`POS.Infrastructure`-only, not part of the `ICashSessionService` Application-layer abstraction — it takes a `PosDbContext`, which Application must never reference) `ICashMovementWriter.WriteSalePaymentAsync`/`WriteRefundPaymentAsync`, implemented by the same `CashSessionService` instance (registered under both interfaces from one DI registration), adds the `CashMovement` row directly to the *caller's own* `db`/transaction — before `SaveChangesAsync`/`tx.CommitAsync` — so the invoice, its payment, and the cash-drawer entry commit or roll back together. Idempotent by `(PaymentId, Type)` so a retried transaction can never double-record the same payment. **Superseded the same day, later session (see the dated section below): "no open session → skip silently, sale still succeeds" was replaced with "no open, authorized session → reject the cash sale/refund outright."**
- **Closing a cash session validates completeness.** **Superseded the same day, later session:** the original rule here (reject closing while the register has *any* Open invoice) turned out to be too strict — see the dated section below for the corrected rule (block only an actual write racing the close, via optimistic concurrency, not every held invoice).
- **A second, parallel actor-misattribution bug fixed (pull side, not just the push side fixed earlier the same day):** `InvoiceSyncService.ResolveInvoiceUserIdAsync` (the WPF client's own apply-a-pulled-invoice path) silently fell back to `_session.UserId` — whichever local user happens to be running sync right now — whenever a pulled invoice's username didn't resolve locally, exactly the same misattribution class already fixed for `/api/sync/invoices/push` earlier the same day, just on the opposite side of the wire and previously unnoticed. Fixed to mirror that exact rule: a username that doesn't resolve returns `null` (never a fallback to the caller's own identity — a competing claim must never be silently overridden), and `PullRemoteInvoicesAsync` now skips applying that invoice (same eventual-consistency behavior the pre-existing "missing product" skip case already uses; the pull cursor still advances, and the invoice applies correctly on a later pass once `PullRemoteUsersAsync` — which already runs first in the background service's ordering — has brought the user down). New test: `InvoiceSyncServiceTests.Pull_remote_invoices_skips_rather_than_misattributes_when_the_username_does_not_resolve_locally`.
- **The device-ID reassignment integrity bug is fixed, in both mirror copies:** `UpsertDeviceSnapshotAsync` (`POS.Api/Program.cs` and its WPF-side twin in `InvoiceSyncService.cs`) used to reassign an *existing* device row's primary key (`device.Id = incoming.DeviceId`) whenever a snapshot matched that row by `Name` but carried a different `DeviceId` — silently dangling every FK already pointing at that row's original `Id` (`Invoices.DeviceId`, `Register` bindings, cash-session history) and letting an unauthenticated/buggy payload repoint an existing device's identity purely by sharing its display name. Fixed: a name-matched row is still found (required — `Devices` has a real `(StoreId, Name)` unique index, so a second row of the same name can't just be inserted instead) and still gets its fields refreshed, but its `Id` is never touched; only a genuinely new row (no `Id` match at all) is created under `incoming.DeviceId`. The pre-existing staleness guard (`device.UpdatedAt > incoming.UpdatedAt` → skip entirely, no row touched or created) applies identically whether the row was matched by `Id` or by `Name`. Verified against the two existing regression tests for the stale-snapshot case (`InvoiceSyncServiceTests.Pull_remote_devices_skips_older_same_name_snapshot_without_changing_local_device_identity`, `ApiIntegrationTests.Sync_devices_push_endpoint_skips_older_same_name_snapshot_without_changing_server_device_identity`) — both already asserted "no identity change," so they caught an initial overly-blunt version of this fix (an early attempt that dropped the name-based lookup entirely broke the unique-index case) before landing the final, correct version.
- **Device-based synchronization now keeps running after logout (tenant.md §5a):** previously every one of the ~16 push/pull methods on `InvoiceSyncService` began with `if (!_session.IsAuthenticated) return ...` and `TryCreateAuthorizedClientAsync` required a cached interactive password — so `ICurrentSession.Clear()` on logout (`StoreId`/`TenantId`/`Password` all wiped) stopped background sync outright, with no way for a per-request device token to help since nothing ever resolved a scope to run under. Fixed with the smallest change that preserves every existing, well-tested method body: `ICurrentSession` gained `HasSyncScope` (`StoreId != Guid.Empty` — true for either an interactive login or a restored device scope) and `SetDeviceSyncScope(tenantId, storeId)` (restores only `TenantId`/`StoreId`, never `UserId`/`Username`/`Password`/permissions/`IsAuthenticated` — grants no interactive access). New `InvoiceSyncService.EnsureSyncScopeAsync()` restores that scope from the local database's single `Store` row (a WPF install is bound to exactly one store by construction) whenever no interactive session is active, and `InvoiceSyncBackgroundService` calls it once at the start of every pass, before the existing 16 calls. All 16 guards were mechanically changed from `!_session.IsAuthenticated` to `!_session.HasSyncScope` — the method bodies themselves, and the `GetCursorAsync`/`UpsertCursorAsync`/`GetTenantIdAsync` helpers they already used, needed no changes at all since they already read `_session.StoreId`/`TenantId`, which are now correctly populated either way. `TryCreateAuthorizedClientAsync` falls back to a new `TryCreateDeviceAuthorizedClientAsync` when `_session.Password` is empty: it looks up this machine's own enrolled `Device` row (matched by `ICurrentDevice.Name`) and its persisted `Sync.DeviceSecret` setting (written by `DeviceManagementViewModel.PersistDeviceSecretAsync` at enrollment time), then authenticates via the existing `/api/devices/token` endpoint exactly as a real Box would. This intentionally does not change what a device token can *do* — the API's existing `SyncPolicy` still only opens 9 of the sync endpoints (invoice/audit-log push+pull, categories/products/settings/users/devices pull) to a device token, so administrative pushes and `currency-policy/pull` correctly keep failing softly (existing non-success handling, no crash) until a real user signs back in; sync doesn't fully stop after logout anymore, but it also doesn't silently gain write privileges it shouldn't have. New test: `InvoiceSyncServiceTests.Push_unsynced_invoices_authenticates_with_the_device_token_endpoint_after_logout_when_this_machine_is_enrolled` (simulates a real `ICurrentSession.Clear()`, proves `EnsureSyncScopeAsync` restores the store scope without restoring `IsAuthenticated`, and proves the push attempt reaches `/api/devices/token` with this machine's real device id/secret instead of `/api/auth/login`).
- **WPF cash-session UI landed** (was entirely missing before this pass — the service layer existed but no client could drive it): new `CashSessionWindow`/`CashSessionViewModel`, cloning the existing `DeviceManagementWindow` pattern. A new sidebar "Cash" entry (deliberately **no permission gate** — any signed-in cashier manages their own till, matching `CashSessionService`'s own authorization rules, which already restrict close/manual-movement actions to the session's own opener in per-cashier mode) opens it. Supports: opening a session (float amount), a running summary of totals (opening float, cash sales, refunds, cash in/out, expected balance) refreshed from `ICashSessionService.GetSummaryAsync`, a movements ledger `DataGrid`, manual cash-in/cash-out entry (with an approving manager's username, resolved to a user id, for the cash-out-approval-required policy), and closing (counted amount + notes, showing the resulting discrepancy). Fully localized EN/AR/HE following the existing `Locale.Get`/resx convention, with RTL flow direction applied like every other window. **`POS.Web` still has no equivalent screen** — deliberately out of scope for this pass (see below).
- **Tests:** 3 new tests, all verified to fail against the pre-fix code before the fix was reapplied — `CashSessionServiceTests.Closing_a_session_is_rejected_while_its_register_has_an_open_invoice`, `InvoiceSyncServiceTests.Pull_remote_invoices_skips_rather_than_misattributes_when_the_username_does_not_resolve_locally`, `InvoiceSyncServiceTests.Push_unsynced_invoices_authenticates_with_the_device_token_endpoint_after_logout_when_this_machine_is_enrolled`. The device-ID reassignment fix and the atomic cash-movement rewrite are covered by the existing, unmodified regression suite (the two same-name-snapshot tests above, and `CashSessionServiceTests.Cash_sale_and_refund_payments_are_recorded_against_the_open_session_without_blocking_the_sale`, which now exercises the fully atomic write path instead of the old best-effort one). Full `POS.Tests` suite: **128/128 passing** (125 baseline + 3 new). `dotnet build` on the full solution (including `POS.Wpf`) is clean, 0 warnings/0 errors.
- **`POS.Web` has no cash-session UI** (WPF-only — carried forward, still true after this session's later work too).

**T4/T4.5 follow-up (2026-09-21, later same-day session) — cash sessions now require an authorized session, closing is concurrency-safe rather than over-strict, CashSession/CashMovement/Register actually sync, a real cross-tenant data leak is fixed, and a full offline→reconnect workflow is verified end to end:** the project owner reviewed the prior pass's design and asked for three corrections plus continued T4 work. Reconciled against the actual code (not assumed from the prior write-up) and implemented:

1. **Cash payments/refunds now require an open, authorized session — the earlier "skip silently" behavior was wrong.** `ICashMovementWriter.WriteSalePaymentAsync`/`WriteRefundPaymentAsync` now return `(bool Success, string? Error)`; when no session is open (or, in per-cashier mode, the open session belongs to someone else), the sale/refund is rejected outright with a clear message instead of completing with a missing cash movement. `SaleService.CompleteCashSaleAsync`/`RefundInvoiceAsync` check this result and roll back the whole local transaction (invoice, payment, stock) on failure — the invoice is left untouched (still `Open`/`Paid`) so the cashier can open a session and simply retry. This only applies to `PaymentMethod.Cash` — a non-cash payment method (none exist yet, but the check is method-gated, not blanket) is a no-op here, never blocked. Both opening a session and completing a sale against one remain purely local operations with no network dependency, so this is fully available offline.
2. **Session closing no longer blocks on every open invoice — only on an actual race.** The prior pass's "reject closing while the register has *any* Open invoice" rule was too strict: a held/parked invoice with no payment in progress should not prevent closing, since its eventual sale will simply be recorded against whichever session is active *when it actually completes* (already-correct existing behavior, now confirmed by a dedicated test). What must be prevented is a payment/refund/manual movement committing against *this exact session* after `CloseSessionAsync` has already read its movements and computed the expected balance — that race, not "any held invoice," is what "block closing when financial operations are incomplete" actually means. Fixed with real optimistic concurrency: `CashSession.SyncVersion` is now an EF concurrency token (`CashSessionConfiguration`, no schema change — `IsConcurrencyToken()` only changes generated `WHERE` clauses), bumped by a shared `TouchSessionForWrite` helper on every write that touches a session (sale/refund movement, manual movement, close). `CloseSessionAsync` and `RecordManualMovementAsync` both wrap their read-compute-write cycle in a bounded retry loop (5 attempts) that catches `DbUpdateConcurrencyException` and reloads fresh state; `SaleService`'s cash-sale/refund paths catch the same exception around their own `SaveChangesAsync`/`tx.CommitAsync` and roll back with a clear "session was closed while completing this — reopen and retry" error. New tests: `CashSessionServiceTests.Closing_a_session_succeeds_while_an_unrelated_open_invoice_has_no_payment_in_progress` (replaces the now-incorrect prior test), `.Completing_a_cash_sale_is_rejected_without_an_open_cash_session`, `.Refunding_a_cash_invoice_is_rejected_when_no_cash_session_is_open_at_refund_time`, and `.A_stale_cash_session_write_is_rejected_instead_of_silently_overwriting_a_concurrent_change` (a deterministic test of the concurrency-token mechanism itself: captures a stale snapshot, lets a real write commit, then proves committing the stale snapshot throws `DbUpdateConcurrencyException` rather than silently winning).
3. **CashSession and CashMovement now actually sync — previously Invoice sync alone left them entirely local.** Modeled as one atomic aggregate per session (header + every movement), the same way Invoice already carries its Items/Payments: `CashSessionSyncDto`/`CashMovementSyncDto` (`POS.Application/Models/InvoiceSyncDtos.cs`), new `PushUpdatedCashSessionsAsync`/`PullRemoteCashSessionsAsync` (client, `InvoiceSyncService.cs`) and `POST /api/sync/cash-sessions/push` / `GET /api/sync/cash-sessions/pull` (server, `Program.cs`), using the same `SyncChange`-sequence cursor mechanism as Category/Device/Product. SyncVersion-based conflict precedence (like Invoice, not the simpler `UpdatedAt` rule): a newer snapshot applies, an equal one is marked synced and skipped, and — the "preserve closed-session history" requirement — a stale (lower-version) snapshot is rejected as a `Conflict` and can never resurrect or overwrite a session that has since closed. Movements are append-only, so reconciling them on apply only ever inserts whichever incoming movement isn't already present locally by its own `Id` — verified idempotent: pushing the identical snapshot twice records the movement once, and pulling the identical version twice is a no-op. Every actor reference (`OpenedByUsername`, `ClosedByUsername`, each movement's `PerformedByUsername`/`ApprovedByUsername`) is resolved by username on apply, never trusted as a raw cross-device id — mirroring the existing invoice-actor fix — and an unresolved session-level actor skips the whole session (retried later) while an unresolved movement-level actor skips just that movement (the session's own authoritative `ExpectedCashAmount`/`DiscrepancyAmount`, computed once at close time, still applies). A `CashMovement`'s `InvoiceId`/`PaymentId` reference is only followed if that row already exists locally, again skip-and-retry rather than fail — this repo's established pattern for a real (if rare) sync-ordering gap.
   - **A real, necessary prerequisite discovered and fixed along the way: `Register` had no sync surface of its own at all.** `CashSession.RegisterId` is a required, `Restrict`-enforced FK — a Register created locally via `DeviceManagementService.ProvisionDeviceAsync` (a fresh `Guid.NewGuid()`, not the deterministic-backfill id) would never have reached the server, so pushing any CashSession referencing it would have failed outright. Added `RegisterSyncDto` and `PushUpdatedRegistersAsync`/`PullRemoteRegistersAsync` (client) plus `POST /api/sync/registers/push` (gated like Device push, `ManageSettingsPolicy`) / `GET /api/sync/registers/pull` (gated like Device pull, `SyncPolicy`) — `UpdatedAt`-precedence, matching Device/Category. Wired into `InvoiceSyncBackgroundService.ExecuteAsync` in the correct dependency order: Registers push/pull right after Devices, CashSessions push/pull right after Invoices (so the FKs a movement/session references already exist server-side by the time it's pushed, and already exist locally by the time a pulled session is applied, within the same pass).
   - **A real, pre-existing tooling gap found and fixed while generating this migration: the EF migration snapshot (`PosDbContextModelSnapshot.cs`) had never been regenerated after the prior session added the `Register`/`CashSession`/`CashMovement` entities** — it was missing those three entities entirely, which would have produced a bogus ~340-operation diff (the exact class of gotcha already documented from an earlier session) the next time anyone ran `dotnet ef migrations add`. Fixed by generating a new migration (`CashSessionConcurrencyToken`, empty `Up`/`Down` — `IsConcurrencyToken()` needs no DDL) that also regenerates the snapshot correctly; verified the full migration history still produces valid PostgreSQL DDL via `dotnet ef migrations script` against the Npgsql provider (static check only — see the PostgreSQL note below).
   - **Tests:** `ApiIntegrationTests.Cash_session_sync_pushes_the_full_aggregate_is_idempotent_and_preserves_closed_history` (real end-to-end HTTP test against the live API: pushes a fresh Register, pushes/re-pushes a CashSession with a movement and confirms no duplicate row, pulls it back and confirms the actor resolved by username, closes it, then proves a stale replay of the pre-close snapshot is rejected as a Conflict and never overwrites the closed state) plus two client-side tests (`InvoiceSyncServiceTests.Push_updated_cash_sessions_sends_the_full_aggregate_with_resolved_usernames`, `.Pull_remote_cash_sessions_applies_the_aggregate_and_skips_rather_than_misattributes_an_unresolved_actor`) exercising the real `CashSessionService`/`InvoiceSyncService` code paths against a mocked HTTP layer.
4. **T4 completion pass — a real, verified cross-tenant data leak fixed, not just the previously-flagged `categories/pull` gap:** auditing the shared `GetGuidSyncChangesAsync`/`GetKeySyncChangesAsync`/`BuildSyncChangeQuery` helpers (used by every sync-pull endpoint) found that their "tenant-wide aggregate" code path (`storeId == null`, used by Category/Product/CurrencyPolicy — recorded with `StoreId = null` in `SyncChange` since they aren't store-scoped) filtered on `StoreId == null` alone, with **no `TenantId` filter anywhere** — so any authenticated user from any tenant could pull every other tenant's categories/products through `/api/sync/categories/pull` and `/api/sync/products/pull`. `categories/pull` additionally took no `ClaimsPrincipal` at all (confirming the T0/T1-era note that flagged it) and `categories/push` derived its tenant via a `ResolveSoleTenantIdAsync` "sole bootstrap tenant" fallback despite already requiring a real JWT (`ManageProductsPolicy`) — both now use the caller's actual claims. Fixed at the source: `BuildSyncChangeQuery` (and its two callers) gained a required `Guid tenantId` parameter, filtering `SyncChange.TenantId == tenantId` unconditionally — a no-op for every store-scoped aggregate (a Store already belongs to exactly one Tenant) but the actual fix for the null-storeId case. All 9 server-side call sites updated (Invoice, CashSession, Category, Product, Setting, User, Device, Register, AuditLog pulls) plus the entity-level queries themselves (`categories/pull`, `categories/push`, `products/pull`, `products/push` now also filter their own `TenantId == tenantId`, belt-and-suspenders). `ResolveSoleTenantIdAsync` — now unused — was deleted rather than left dead. **Verified as a real bug, not assumed:** `ApiIntegrationTests.Categories_pull_never_leaks_another_tenants_categories` provisions a genuinely second tenant via the existing `/api/platform/tenants` flow and proves its pull cannot see the bootstrap tenant's seeded categories; this test was run against the pre-fix code first and failed exactly as expected (both "Beverages" and "General" leaked through), then passed once the fix was restored.
5. **A full offline-to-reconnect workflow verified end to end, not just each piece in isolation:** `EndToEndWorkflowTests.Offline_sale_refund_logout_device_sync_reconnect_and_cash_reconciliation` — enrolls this machine's device credential, completes a cash sale and an immediate refund fully offline (no network calls involved at all), logs the employee out, confirms background sync continues under the device credential alone (`/api/devices/token`, not `/api/auth/login` — zero user logins recorded) and successfully pushes the offline invoice, then has the employee reconnect (sign back in) and confirms a subsequent sync pass now authenticates as that user again (`/api/auth/login`, exactly once) and pushes the cash session, and finally closes the session and confirms the sale-then-refund nets to exactly zero discrepancy. Each phase runs in its own DI scope, matching how `InvoiceSyncBackgroundService` creates a fresh scope (and therefore a fresh, uncached authorized `HttpClient`) per real background pass — an earlier draft of this test reused one scope throughout and silently reused the cached device-authorized client after "reconnecting," which would have hidden exactly the kind of stale-credential bug this test exists to catch.
6. **PostgreSQL live rehearsal remains explicitly pending, as instructed.** No Docker or `psql` is available in this environment (checked directly, not assumed). What was verified instead, same as before: `dotnet ef migrations script` against the full history with the Npgsql provider forced completes without error and produces syntactically plausible PostgreSQL DDL, including the new `Registers`/`CashSessions`/`CashMovements` tables and the `CashSessions(RegisterId) WHERE "Status" = 0` filtered unique index. This is static evidence only — it cannot catch a real connection/extension/permission/runtime issue — and a live rehearsal against an actual PostgreSQL instance is still the open item, tracked explicitly rather than silently marked done.
- **Tests:** 6 new/replaced tests this pass (4 in `CashSessionServiceTests`, 3 in `InvoiceSyncServiceTests`, 1 in `ApiIntegrationTests` for the cash-session sync round trip, 1 in `ApiIntegrationTests` for the cross-tenant leak, 1 new `EndToEndWorkflowTests` file) — full count below. Full `POS.Tests` suite: **136/136 passing** (128 baseline + 8 net new). Full solution `dotnet build` (including `POS.Wpf`) is clean, 0 warnings/0 errors.
- **T4 status: substantially complete, not marked done outright.** What's covered: scoped sync (the cross-tenant leak above, the register/cash-session FK-ordering fix), authorized sync (device-token scope boundaries from the prior session, cash-session write authorization), and replay-safe sync (invoice idempotency from Stage 4.4, the new cash-session idempotency proven above). What's explicitly **not** claimed as done: a full audit of every remaining sync endpoint for the same class of gap this pass found in Category/Product (Settings/User/Device/AuditLog/Register were all store-scoped already and are very likely fine, but "very likely" is not "audited line by line" the way Category/Product now are); tombstone/hard-delete propagation across the sync engine generally; and the live PostgreSQL rehearsal. T5 (tenant-aware administration and branch access), T6 (isolation/resilience release gate), and T7 (recovery rehearsal/pilot) were not started this session — the scope of this pass was deliberately the concrete corrections the project owner asked for plus the specific, already-flagged T4 gap, not a full sweep through the remaining milestones in one session.

**T4/T6 follow-up (2026-09-21, new session) — cash-session conflict handling and actor authorization verified and fixed, sync isolation review completed with two real cross-tenant bugs fixed, T5 audited (found essentially unstarted), T6 matrix coverage assessed:** the project owner asked for (1) verification that cash-session write ownership and conflict handling never discards legitimate movements or reopens a closed session, with competing-update coverage or an enforced single-writer rule; (2) actor resolution that is tenant-scoped, survives a username change, and validates the actor's *and approver's* authorization rather than treating a resolved username as proof of it; (3) completion of the sync isolation review (nested references, deletion/deactivation propagation, replay safety); (4) continuing T5–T7 in order while keeping the live PostgreSQL rehearsal explicitly pending. Each was checked against the real code, not assumed from the prior session's write-up:

1. **Two real cash-session conflict-handling bugs found and fixed, both in the server push handler (`Program.cs`) and its client-side pull-apply mirror (`InvoiceSyncService.cs`):**
   - **Movement loss on a Skipped/Conflicted header.** `ReconcileCashMovementsAsync` (which inserts whichever incoming `CashMovement` rows aren't already present locally by Id — append-only, idempotent) was only ever called on the "header applied" and "new session created" branches. A header that came back `Skipped` (a version tie, e.g. a retried push) or `Conflict` (a stale header) silently discarded every movement in that same payload, even movements the server had never seen — a genuine violation of "version precedence must not discard legitimate movements." Fixed by reconciling movements unconditionally before the header's version outcome is decided, on both sides.
   - **A closed session could be reopened by a higher-SyncVersion payload.** The header-apply branch (`existing.SyncVersion < incoming.SyncVersion`) copied `Status` (and every other field) from the incoming payload with no check that the *existing* session was already `Closed` — a device that fell out of sync before learning about a close, and later pushed/received a payload claiming a higher version while still `Open`, would have silently reopened the session and overwritten its closing figures. Fixed: once `existing.Status == Closed`, no incoming payload — however high its version — may change the header; it is rejected as `Conflict` (server) / skipped (client) instead. Verified this is the *narrower*, correct fix by keeping the original `>`/`==` branches (and their existing `Conflict`/`Skipped` labels) untouched — an early draft applied the closed-check before the version comparison and flipped an existing, correct `Conflict` result to `Skipped` for an ordinary stale replay, breaking `Cash_session_sync_pushes_the_full_aggregate_is_idempotent_and_preserves_closed_history`; caught immediately by the full suite, not shipped.
   - **Single-writer analysis:** a `Register` has exactly one *active* `Device` bound to it at a time (`DeviceManagementService.ProvisionDeviceAsync` refuses a second active device on the same register), so true concurrent writers to the same `CashSession` from two different physical machines is not a reachable steady-state scenario by construction — the realistic case is a device that fell behind (network gap, hardware-swap handover) and replays stale or leapfrogged state, which is exactly what the two fixes above close off. This is documented here as the actual single-writer justification rather than left implicit.
   - **Tests (6 new, all verified against the pre-fix code first):** `ApiIntegrationTests.Cash_session_sync_never_reopens_a_closed_session_even_with_a_higher_sync_version`, `.Cash_session_sync_never_discards_a_movement_because_the_header_was_skipped_or_conflicted` (exercises both the Skipped and Conflict branches directly), `InvoiceSyncServiceTests.Pull_remote_cash_sessions_never_reopens_a_session_this_device_already_closed`, plus the two actor/approver tests below.
2. **Actor resolution: tenant-scoped (already true), now also survives a username change, and approver authorization is genuinely validated, not just resolved.** `CashMovementSyncDto`/`CashSessionSyncDto` gained id fields (`PerformedByUserId`, `ApprovedByUserId`, `OpenedByUserId`, `ClosedByUserId`) sent alongside the existing usernames. `ResolveCashActorUserIdAsync` (both server and client copies) now tries the claimed id first — accepted only once verified against a real, active, tenant/store-scoped user (never trusted on its own) — falling back to the username match for payloads that carry no id. This closes a real gap: previously, an offline device's pending cash-session push would permanently fail to apply (`"Could not verify a tenant user for ..."`) if the actor's username was changed server-side before the device reconnected, even though the same underlying user still existed. Separately, and more importantly: **a resolved approver username was previously accepted as sufficient proof of authorization for a `CashOut` approval** — the sync-apply path had no equivalent of `CashSessionService.RecordManualMovementAsync`'s local rule that an approver must be a *different* person who currently holds `ManageSettings`. A malicious or buggy client could have pushed a cash-out "approved" by the same cashier who performed it, or by an unrelated low-permission user, and it would have been recorded into the ledger as a valid approval. Fixed with a new `IsAuthorizedCashApproverAsync` check (server and client), applied to every movement carrying an approver, mirroring the local rule exactly. **Tests (3 new):** `ApiIntegrationTests.Cash_session_sync_rejects_a_cash_out_movement_with_a_self_approval_or_an_unauthorized_approver` (proves self-approval, an unauthorized approver, and a genuinely authorized approval are each handled correctly in one push), `.Cash_session_sync_resolves_the_actor_by_id_when_the_username_no_longer_matches`.
3. **Sync isolation review completed** (delegated to a research-only Explore pass across every non-CashSession sync aggregate, then independently verified and fixed by this session before trusting any finding):
   - **Two real, exploitable cross-tenant bugs found and fixed**, both via a *nested* reference inside an otherwise-valid, correctly-authenticated request from tenant A — exactly the T6 exit scenario "reference B's product/customer/role/device inside A's valid request":
     - `POST /api/sync/invoices/push`: an invoice line's `ProductId` was checked for existence with **no `TenantId` filter** (`db.Products.Where(!IsDeleted)`), so a line could reference another tenant's real product id and be accepted, writing a cross-tenant FK into `InvoiceItems`. Fixed by scoping the existence check to the caller's own tenant.
     - `POST /api/sync/products/push` → `EnsureCategoryAsync`: a product's `CategoryId` was resolved by **id alone, with no `TenantId` filter** — worse than a read leak, this let tenant A's product push silently **overwrite tenant B's real category's Name/UpdatedAt in place** if the id happened to collide (a guessed or, in principle, an accidentally-reused GUID). Fixed by scoping the lookup to the caller's tenant; a genuine id collision with another tenant's row now fails on the database primary key instead of silently corrupting it. **Known rough edge, not fixed this pass:** because every bulk push endpoint (`products/push` included) calls `SaveChangesAsync` once after its whole per-item loop rather than per item, a single item's DB-level failure (like this now-correctly-rejected collision) throws past every item's own `try/catch` and fails the **entire batch** with an unhandled exception (HTTP 500) instead of a clean per-item `"Failed"` result — the transaction still rolls back cleanly (verified: no corruption, nothing commits), so this is a safe failure mode, not a security gap, but it means one poisoned item can block an otherwise-healthy batch from making any progress. A proper fix (per-item `SAVEPOINT`s) needs verification against real PostgreSQL's stricter "transaction aborted after any error" semantics, which this environment cannot rehearse — tracked here rather than attempted blind.
     - `GET /api/sync/currency-policy/pull` also lacked the `TenantId` filter the earlier Category/Product leak fix added everywhere else (it has its own inline query rather than using the shared helper). Fixed. Impact was limited (the actual policy body already comes from a store-scoped service call) but the sync-cursor "is there anything new" signal was cross-tenant.
   - **Deletion/deactivation propagation reviewed and confirmed correct** for Category, Product, User, Device (including the existing one-way "sticky" revocation), Register, and Settings — `IsDeleted`/`IsActive`/`IsRevoked` all round-trip through the sync DTOs and apply on both push and pull.
   - **Replay safety reviewed and confirmed idempotent-by-design** for Invoice, Category, Product, Register, Settings, User (all guarded by `SyncVersion` or `UpdatedAt` precedence) and AuditLog (append-only, deduped by Id). **Left explicitly unverified:** true *concurrent* (not sequential-retry) double-push races — e.g. two simultaneous pushes for the same invoice interleaving across their separate transactions — were not checked against the actual DB isolation level/locking behavior on either SQLite or PostgreSQL; flagged rather than assumed safe.
   - **Barcode uniqueness** (a named T6 matrix concern, "same normalized barcode in A and B → independent records") reviewed: there is no unique constraint on `Product.Barcode` at all today, globally or per-tenant, at the DB or application level — duplicates are already possible within a single tenant. No cross-tenant reconciliation logic keys off barcode anywhere, so two tenants sharing a barcode cannot merge by construction; inventing a new per-tenant uniqueness constraint was deliberately not done, since nothing in the current catalog requires one-barcode-per-product identity and tenant.md frames this as conditional ("when the current catalog requires it"), not mandatory.
   - **SQLite foreign-key enforcement (tenant.md §4's explicit "enable and verify" instruction) checked empirically, not assumed:** `PRAGMA foreign_keys` reads `1` (on) on the real connection this app opens, and a raw `INSERT` into `InvoiceItems` referencing a nonexistent `InvoiceId`/`ProductId` — bypassing every application-level check entirely — is rejected by SQLite itself with a `SqliteException`. This is a real DB-level backstop underneath the two application-level nested-reference fixes above, not just a paper requirement.
   - **Tests (3 new):** `ApiIntegrationTests.Product_push_cannot_overwrite_another_tenants_category_via_a_colliding_categoryId`, `.Invoice_push_rejects_a_line_referencing_another_tenants_product` (both provision a real second tenant via `/api/platform/tenants` rather than faking tenant boundaries), `.Database_foreign_key_enforcement_is_enabled_and_rejects_an_orphaned_reference`.
4. **T5 (tenant-aware administration and branch access) audited, found essentially unstarted — not implemented this pass.** A dedicated code audit (not a docs re-read) found: every user (Web/API/WPF) is still hard-bound to exactly one `Store` via `User.StoreId`; `UserStoreAccess` (added back in T1) is written once per user creation but **never read or enforced anywhere** in the codebase — no query joins against it; there is no store-switcher UI or active-store selector anywhere; there is no tenant-admin "manage my stores" screen (Store creation is platform-provisioning-only, via `TenantProvisioningService`); `POS.Web`'s `ManagementController.UpdateStoreProfile` only edits the caller's own single store's profile fields; and there is no export/download/background-report-job surface in `POS.Web` at all yet, so "scope exports/background jobs by tenant/store" has nothing to scope. What *is* solid: permission-mask-based server-side authorization (`WebAuthorizationPolicies`) is real and correctly enforced, satisfying the "cashier/report restrictions enforced server-side" clause of T5's exit criterion, but only within a user's single store — the "managers see only assigned stores" and "tenant admins manage their own stores" clauses have no feature to satisfy them yet. **This is a genuine, multi-part feature (multi-store grants + UI, a store-switcher, tenant-facing Store CRUD) that was deliberately not attempted as a rushed addition to this session** — building it carelessly in a multi-tenant authorization context is exactly the kind of shortcut this whole workstream exists to avoid. It remains `[ ]` not started; a future session should scope it as its own focused pass.
5. **T6 (isolation and resilience release gate) — partial matrix coverage assessed against the actual required-coverage table in tenant.md, not assumed complete.** Rows with real, verified test coverage as of this session: cross-tenant category/product pull leak and the two new nested-reference pushes above ("reference B's X inside A's valid request"); same username independent across two tenants (`AuthServiceTests`); device token rejected from business operations and admin sync pushes (`ApiIntegrationTests.Device_token_authenticates_sync_but_is_rejected_from_business_operations`); invoice sync payload attributing a sale to a nonexistent/cross-tenant username rejected (both push and pull directions); retry-after-commit-before-ack for invoices and cash sessions (SyncVersion-guarded idempotency); guess/reference another tenant's Register or CashSession id (store-scoped queries throughout `CashSessionService`, plus the new closed-session and approver tests); offline authorization expiry and pending-work retention after revocation (`OfflineAuthorizationPolicyTests`). **Rows with no coverage, explicitly not claimed as done:** "use A1 cashier rights to access A2" (blocked on T5 — there is no second store-assignment concept yet to test); concurrent offline edits and last-unit sales (no test exercises two simultaneous offline sales against the same product's last unit — the local no-negative-stock behavior exists per Stage 1/3 but the *global* oversell-reconciliation policy this row asks for is undocumented and untested); web stock adjustment followed by device reconnect ledger reconciliation; cached reports/exports/job store-switch correctness (moot until T5 builds any of that surface); migration-preserves-counts-and-invariants row (covered only by the SQLite rehearsal already on record, not PostgreSQL). T6 remains `[ ]` not started as a completed gate — this session materially added to its evidence base without closing it.
6. **T7 (recovery rehearsal and controlled pilot) — explicitly not started, blocked on infrastructure this environment does not have.** No Docker/PostgreSQL instance, no backup/restore tooling exercised, and no real or test pilot business exists to rehearse against. Nothing in this pass should be read as progress on T7; it is recorded here only to keep the gate explicit rather than silently absent.
7. **PostgreSQL live migration/runtime rehearsal remains explicitly pending, unchanged from the prior session, as instructed.** Only the static `dotnet ef migrations script`-against-Npgsql check exists; no live PostgreSQL instance has been used at any point in this project's history to date.
- **Tests:** 8 new this pass — 2 conflict/reopen tests + 2 actor/approver-authorization tests in `ApiIntegrationTests`, 1 conflict/reopen test in `InvoiceSyncServiceTests` (all 5 named in point 1/2 above), plus the 3 isolation tests named in point 3 (2 nested-reference fixes + the FK-enforcement verification). Full solution build clean (0 warnings/0 errors); full `POS.Tests` suite: **144/144 passing** (136 baseline + 8 new). Every new test targeting a fix was confirmed to fail against the pre-fix code before that fix was applied; the FK-enforcement test is a verification, not a regression test for a bug found this pass.

**Concurrent double-push fix, and T5 (tenant-aware administration and store access) implemented (2026-09-22, new session):** the project owner asked for two things in order: (1) address the specific "true concurrent double-push" gap the prior session flagged as explicitly unverified, with a focused test and any necessary fix, accepting that authoritative confirmation may need a live PostgreSQL rehearsal; (2) implement T5 — authorized multi-store access, store switching, tenant-scoped Store CRUD, server-side enforcement, and preservation of the enrolled device's store binding and pending local transactions — plus the T6 matrix rows T5 unblocks.

1. **The concurrent double-push gap, diagnosed and fixed.** Traced the actual risk (not assumed): `Invoice.SyncVersion` was an ordinary column, not an EF concurrency token, so `/api/sync/invoices/push`'s update path (`existing.SyncVersion < incoming.SyncVersion` → apply) generated a plain `UPDATE ... WHERE Id = @id` with no version guard. Two genuinely concurrent pushes of the *same* invoice update (e.g. a lost-ack retry racing the still-in-flight original push) that both read the invoice before either commits would both pass that comparison, both apply the same `Paid` transition, and both call `ReconcileInvoiceInventoryAsync` — which computes `Inventory.Quantity` as an absolute value from the (identical, stale) starting point, so the balance itself happens to end up correct by coincidence, but each racer unconditionally inserts its *own* new-Guid `StockMovement` row, producing a duplicated stock-ledger entry for one real sale. The new-invoice insert path is separately protected by `Invoice.Id` being the primary key (a genuine simultaneous insert-insert race blocks/fails at the DB and rolls back cleanly), so the update path was the actual gap. Fixed: `InvoiceConfiguration.SyncVersion` is now `.IsConcurrencyToken()` (mirroring the existing `CashSession.SyncVersion` fix from an earlier session — no DDL, an empty migration `InvoiceSyncVersionConcurrencyToken` keeps the model snapshot in sync, verified via `dotnet ef migrations script` against the full history on the Npgsql provider). The push handler's final `SaveChangesAsync`/`CommitAsync` now catches `DbUpdateConcurrencyException` (the losing racer's stale write) and `DbUpdateException` (the rarer raw-constraint/lock-conflict shape of the same race) and rolls back cleanly, returning `409 Conflict` instead of leaking an unhandled 500 — the client's existing "non-success response leaves these invoices unsynced, retry next pass" behavior (`InvoiceSyncService.PushUnsyncedInvoicesAsync`) already handles this correctly with no client-side change needed, and the next retry correctly resolves via the ordinary `SyncVersion` equality ("Skipped") path once the winner's commit is visible.
2. **Tests, honestly scoped.** `ApiIntegrationTests.Invoice_concurrency_token_rejects_a_stale_concurrent_update_instead_of_silently_reapplying_it` is the real proof: two `PosDbContext` instances both load the same invoice at `SyncVersion=1` (the exact stale-read starting point two concurrent pushes would share), the first applies and commits, the second — still holding the now-stale original version — throws `DbUpdateConcurrencyException` on `SaveChangesAsync` instead of silently succeeding; deterministic, not timing-dependent, mirrors the established `CashSessionServiceTests` concurrency-token test pattern. `ApiIntegrationTests.Concurrent_retries_of_the_same_invoice_completion_produce_exactly_one_stock_effect` fires several genuinely concurrent HTTP pushes of the identical retry payload through the real endpoint and asserts the outcome invariant (exactly one `StockMovement` row, correct final quantity, no unhandled 500). **Empirically, this second test could not be made to reproduce a true interleaved race even at 40-way concurrency with the fix reverted** — this environment's SQLite-backed `WebApplicationFactory` test host appears to fully serialize these requests (single-writer file locking and/or request scheduling), so it currently passes both with and without the fix and is kept as a real "repeated retries never corrupt state or crash" regression test rather than a proven reproduction. **Whether a genuine interleaved double-push is reachable under real load against PostgreSQL's row-level MVCC (a materially different concurrency model than SQLite's) is exactly what the still-pending live rehearsal must confirm — explicitly left open, not claimed resolved.** Full `POS.Tests`: 146/146 (144 baseline + 2 new) before starting T5.
3. **T5 implemented:** new `Permission.ManageStores` flag (distinct from `ManageSettings`, which only edits the caller's own *active* store's profile — this one governs creating branches and granting store access tenant-wide); `IStoreAccessService`/`StoreAccessService` (`POS.Application`/`POS.Infrastructure`) provides `GetTenantStoresAsync` (tenant-scoped, for admin listing), `GetAccessibleStoresAsync` (a user's home store `UNION` their `UserStoreAccess` grants — both were already written by every user-creation path, just never read before this), `CanCurrentUserAccessStoreAsync` (the actual authorization check — tenant ownership *and* access, never trusts a client-supplied store id alone), `CreateStoreAsync` (new branch, **inherits the tenant's existing base currency automatically** rather than accepting an admin-chosen one — enforces tenant.md §3's "one canonical catalog/base currency per tenant" structurally instead of by convention), `GrantStoreAccessAsync`/`RevokeStoreAccessAsync` (tenant- and store-scoped; revoking a user's own home store is refused, since that requires reassigning the home store first, not this action). New `POS.Web` `StoresController` + `Views/Stores/Index.cshtml`: `Index`/`Create`/`GrantAccess`/`RevokeAccess` require `ManageStores`; `Switch` is available to any authenticated user but **re-validates access server-side on every call** (never trusts the posted store id) and re-issues the auth cookie with only the store/currency claims changed — user, tenant, role and permission claims are untouched, so switching stores never touches what a user is permitted to *do*, only *where*. A store-switcher `<select>` (auto-submitting on change) appears in `_Layout.cshtml`'s header whenever a signed-in user has more than one accessible store, calling `IStoreAccessService.GetAccessibleStoresAsync` per request — acceptable at this app's scale (a control-room dashboard, not high-traffic). `POS.Wpf`'s existing "Role permissions" editor (the *only* place any client can grant a custom role's permissions — `POS.Web` has no role editor) gained the matching `ManageStores` checkbox (EN/AR/HE localized) so a tenant can actually grant this to a non-Admin role; every tenant's seeded/provisioned `Admin` role already has it automatically since `Permission.All` now includes it.
4. **Existing reports/inventory/management screens needed no changes to respect the active store** — `DashboardController` and `ManagementController` already scope every query by `_session.StoreId`, so switching stores automatically re-scopes reports, inventory, and the store-scoped user roster; this is confirmed by code reading, not assumed. `ManagementController.CreateUser` already stamps `UserStoreAccess` for the user's home store (an existing T1-era write path), so new users created while an admin is switched into a branch are correctly home-anchored there.
5. **A real EF query-translation bug found and fixed while building this (not shipped):** `Select(...).OrderBy(x => x.Name)` on the projected `StoreListItemDto` failed to translate ("could not be translated... insert AsEnumerable/ToList") because the constructed-record projection couldn't be reordered through by EF's SQL translator — caught immediately by the first test run (a 500 on every page render once the store-switcher was added to the shared layout, so **every** integration test touching an authenticated page failed, not just the new ones). Fixed by ordering the underlying `Store` query *before* projecting to the DTO, not after.
6. **What's deliberately unaffected — the enrolled device's store binding and pending local transactions:** T5 is scoped to `POS.Web` only, per tenant.md's own T5 bullet list ("adapt existing POS.Web screens... store access management and an authorized active-store selector"). Nothing about `Device`, `Register`, `ICurrentSession.SetDeviceSyncScope`, or the WPF sync worker's per-device credential was touched — a WPF install remains bound to exactly one store by construction (unchanged design from T1/T3), so there is no "pending local transaction" risk to introduce: an enrolled device never gains or loses store access through this feature, and offline sales queued locally are entirely unaffected. Verified by re-running the full suite (includes `BusinessJoinServiceTests`, `RegisterBackfillTests`, `EndToEndWorkflowTests`, `CashSessionServiceTests`) with no change in outcome.
7. **A real upgrade/migration note, not silently glossed over:** because `Permission.All`'s bitmask value changed (gained a 7th bit), only *newly created* roles pick up `ManageStores` automatically via `Permission.All`; an already-existing tenant's `Admin` role, whose `PermissionsMask` is a static integer already persisted in its row, does **not** retroactively gain the new bit — it must be re-saved once through the WPF role editor (or a future Web equivalent) after upgrading. This is the same behavior every prior permission-bit addition in this codebase has had; flagged here so it isn't mistaken for a bug.
8. **T6 matrix rows this unblocks, tested for the first time:** "Use A1 cashier rights to access A2 → Denied unless explicitly assigned and permitted" was explicitly recorded as blocked on T5 in the prior session's entry — now proven end to end (`WebIntegrationTests.Store_switch_is_rejected_without_an_explicit_grant_and_succeeds_once_one_is_made`: a `ViewReports`-only user is refused a switch into a second same-tenant store, verified by the active store's name genuinely not changing on the next page load, then succeeds once — and only once — a `ManageStores` admin grants access through the real `Stores` screen). Cross-tenant variants of the same two matrix rows ("guess another tenant's IDs..." and "reference B's ... inside A's valid request") are covered for stores specifically: `WebIntegrationTests.Store_switch_is_rejected_for_a_store_belonging_to_a_different_tenant`, `StoreAccessServiceTests.Tenant_stores_list_never_includes_another_tenants_store_even_by_id`, `StoreAccessServiceTests.Granting_store_access_rejects_a_cross_tenant_store_or_user` (a real tenant-A user against tenant B's store, and a real tenant-B user against tenant A's store, both rejected in the same test). "Cached reports, exports, jobs and store switches → correct ownership" — the *store switches* clause is now covered; exports/background jobs remain not applicable, since `POS.Web` still has no such surface to scope (unchanged from the prior audit).
9. **What's still open, not claimed as done:** `POS.Web` has no role-permission editor at all (WPF remains the only client that can grant `ManageStores` to a custom role — a real, pre-existing gap this session did not create but also did not close); no per-request re-validation that a still-active session's store access hasn't been revoked mid-session (access is checked at switch time and baked into the signed cookie until it expires/refreshes, the same staleness window every other claim — role, permissions — already has; not a new gap, but not newly closed either); T6 remains an in-progress gate, not a completed one — concurrent offline last-unit sales, a web-stock-adjustment-then-device-reconnect reconciliation test, and the PostgreSQL-backed migration/invariant-preservation row are all still unwritten. T7 and the live PostgreSQL rehearsal remain untouched, explicitly pending as instructed, blocked on no Docker/PostgreSQL in this environment.
- **Tests:** 13 new this pass — 2 concurrency tests (point 2 above) + 7 in `StoreAccessServiceTests` + 4 in `WebIntegrationTests` (point 8 above). Full solution build clean (0 warnings/0 errors, including `POS.Wpf`); full `POS.Tests` suite: **157/157 passing** (144 baseline + 13 new).

**T6 completion pass (2026-09-22, new session) — the four gaps the project owner named explicitly, plus a row-by-row close-out of the rest of the T6 matrix that's reachable without a live PostgreSQL instance:**

1. **Tested upgrade/bootstrap path for existing tenant Admin roles to gain `ManageStores`, without expanding any custom role.** The prior session's note ("an existing tenant's Admin role does not retroactively gain a new bit — must be re-saved once via the WPF role editor") was a real, unfixed gap: nothing but a *dev/demo-only*, single-tenant seeder fixup (`DatabaseSeeder.SeedIfNeeded`, gated behind `Database:SeedDemoDataOnStartup` which defaults to **false** in production) ever touched an existing role's `PermissionsMask`. Added migration `20260922133730_UpgradeAdminRolesManageStores` — a real, always-applied (`db.Database.Migrate()` runs unconditionally under `ApplyMigrationsOnStartup`, independent of demo seeding) data-only migration that runs `UPDATE "Roles" SET "PermissionsMask" = "PermissionsMask" | 64 WHERE "PermissionsMask" = 63` across **every** tenant, not just one. Deliberately value-scoped, not name-scoped: only a role whose mask was *exactly* the pre-`ManageStores` "every other permission" value (63) is touched — a partial/custom role (e.g. `ManageProducts | ManageSettings` = 17) is provably untouched, and a role already at 127 is a no-op, not a double-grant. `Down` is a documented best-effort inverse (127→63), which cannot distinguish a role this migration upgraded from one that reached 127 independently — an accepted, explicitly-noted imprecision, same class as `AddTenantFoundation`'s irreversible backfills. Verified against the *real* migration history on a real SQLite file (`RoleUpgradeMigrationTests`: migrates to the migration immediately before this one, inserts rows including an Admin role at 63 in each of two tenants, a `PermissionsMask=0` Cashier, a partial-permission custom role, and a role already at 127, then migrates to latest and asserts each outcome individually) plus a `dotnet ef migrations script`-against-Npgsql static check (unchanged process from prior sessions, still the only PostgreSQL verification available in this environment) confirming the same `|`/`&` bitwise SQL renders correctly for Postgres too.
2. **Revoking store access is now enforced on the very next protected request, even against an already-issued cookie.** Previously, `UserStoreAccess` was checked only at `StoresController.Switch` time and then trusted for the cookie's full up-to-8-hour sliding-expiration window — an admin's `RevokeAccess` had no effect on a session that was already switched into that store. Added a `CookieAuthenticationEvents.OnValidatePrincipal` handler (`POS.Web/Program.cs`) that re-derives tenant/user/store ids from the cookie's own claims and re-checks them against the database (a new session-independent `IStoreAccessService.CanUserAccessStoreAsync(tenantId, userId, storeId, …)`, since `ICurrentSession` is not yet populated at cookie-validation time — the existing session-populating middleware runs later in the pipeline) on **every** authenticated request, rejecting and signing out the principal if access no longer holds. Deliberately no caching/throttling — a real DB round-trip per request, correctness over latency for an authorization decision at this app's scale (a control-room dashboard, not high-traffic); flagged as the thing to revisit first if request volume ever makes it a bottleneck. Proven end to end, not just at the service layer: `WebIntegrationTests.Revoking_store_access_mid_session_blocks_the_very_next_protected_request` grants, switches, then revokes mid-session with the cookie untouched throughout, and asserts the very next `GET /Management` is redirected to login rather than served.
3. **Sync push endpoints no longer treat every `DbUpdateException` as a retryable 409.** `/api/sync/invoices/push`'s catch-all `DbUpdateException` handler previously returned `409 Conflict` ("Retry this push") for *any* save-time failure, including a foreign-key, check, or not-null violation — a genuine data problem that retrying the identical payload can never fix, which would have looped a client's background sync worker forever. Added `POS.Api.Infrastructure.SyncConflictClassifier.IsRetryable(DbUpdateException)`, a small standalone public class (extracted out of `Program.cs`'s top-level-statement local functions specifically so it's unit-testable from `POS.Tests` — a top-level-statement local function is not externally callable): retryable only for a unique-constraint collision (`SqliteExtendedErrorCode` 2067/1555, or Npgsql `SqlState` `23505`) — the exact shape a genuine concurrent double-insert race produces (two pushes both decided "this row doesn't exist yet," a retry's fresh read now finds it and updates instead) — or a PostgreSQL transaction-level retry signal (`40001` serialization failure, `40P01` deadlock); everything else (FK/check/not-null violations, or any unrecognized exception shape) fails closed as **permanent**, now returned as `422 Unprocessable Entity` instead of `409`. The invoice-push handler's `DbUpdateConcurrencyException` handling (the more common real-world path for this race, from the prior session's fix) is unchanged. Verified against real exceptions from a real SQLite database, not hand-constructed fakes — `SyncConflictClassifierTests` triggers an actual unique-index violation (two `Role` rows with the same `(TenantId, Name)`) and an actual FK violation (a `Role` referencing a nonexistent `TenantId`) via real `SaveChangesAsync` calls and asserts the classifier's verdict on each.
4. **Confirmed: a failed concurrent push rolls back its invoice, inventory, and stock-ledger writes together, not just the invoice row.** This was already structurally true — the endpoint wraps the whole batch in one `BeginTransactionAsync`/one `SaveChangesAsync`/`CommitAsync`, rolling back on either exception path — but nothing proved the *inventory/stock-ledger* part of that claim specifically; the existing deterministic concurrency test only staged an `Invoice.Status`/`SyncVersion` change. Added `ApiIntegrationTests.Failed_concurrent_push_rolls_back_invoice_inventory_and_stock_ledger_writes_together`: the losing `DbContext` in the same two-context race stages the invoice flip **and** an `Inventory.Quantity` decrement **and** a new `StockMovement` ledger row — exactly what a real completion's `ReconcileInvoiceInventoryAsync` would queue — all in the one context/batch, then loses on the invoice's stale concurrency token; a fresh verification context confirms neither the inventory decrement nor the stock-movement row landed. This is an EF Core/ADO.NET guarantee (a `DbContext`'s pending changes are sent as one batch), not new code, but it is exactly the property the real endpoint's single-transaction design depends on, and it is now actually asserted rather than assumed.
5. **T6 matrix, closed out row by row against tenant.md's actual required-coverage table (not assumed complete):**
   - *Now covered, newly this pass:* "stale online token after user/device/access revocation → access blocked" (point 2 above); "close a cash session opened under a different tenant/store → rejected" (`CashSessionServiceTests.Closing_a_cash_session_belonging_to_a_different_tenant_and_store_is_rejected` — a real, valid, open session id belonging to a different tenant's store is indistinguishable from a nonexistent one, matching `CloseSessionAsync`'s existing `s.StoreId == _session.StoreId` scoping, which was already correct but untested); "retry sale/refund after server commit but before acknowledgement → single financial and stock effect" gains the inventory/stock-ledger rollback proof (point 4 above) on top of the prior session's single-stock-effect proof.
   - *Already covered by prior sessions, reconfirmed still passing, not re-explained here:* cross-tenant/nested-reference rejection for products/categories/stores; same username/role-name independent across tenants; device token rejected from business operations and admin sync pushes; invoice payload attributing a sale to a nonexistent/cross-tenant username rejected; guess/reference another tenant's Register or CashSession id; offline authorization expiry and pending-work retention after revocation; "use A1 cashier rights to access A2" and its cross-tenant variants (T5's store-switch tests); "alter... permission mask in sync → rejected" (`ApiIntegrationTests.Sync_push_endpoints_reject_a_user_without_the_matching_management_permission` — a Cashier's device-credential re-login cannot reach `/api/sync/users/push` at all without `ManageUsers`, so a permission-mask tamper attempt is rejected at the endpoint gate, not merely at the field level).
   - *Verified by direct code/schema inspection this pass, not by a new dedicated integration test:* "same product sold at A1 and A2 → shared definition, independent balances" — `InventoryConfiguration` has a `(TenantId, StoreId, ProductId)` unique index, so two stores' `Inventory` rows for the same `Product` are structurally independent and DB-enforced; not exercised end-to-end via two live invoice pushes to two different stores in this pass (would need a second store-scoped authenticated client), so noted as verified-by-inspection rather than claimed as integration-tested. "Cross-tenant database FK insertion attempt → constraint rejects it" — covered by the existing `ApiIntegrationTests.Database_foreign_key_enforcement_is_enabled_and_rejects_an_orphaned_reference` (from the T6/T4 FK-enforcement session) plus this pass's own `SyncConflictClassifierTests` FK-violation case, both against real SQLite FK enforcement (verified on, not assumed).
   - **Still genuinely open, not claimed as done:** "concurrent offline edits and last-unit sales → defined reconciliation, no silent loss or false stock guarantee" — no test exercises two simultaneous offline sales against the same product's last unit, and more importantly there is still no documented *policy* for what should happen (does the second sale apply and let stock go negative, recording both sales' ledger entries honestly, or does it need a hard stock-out guarantee across devices that offline-first sync cannot actually provide?) — this needs a product decision before it can be implemented and tested, not just a bug fix, and was deliberately not invented unilaterally in this pass. "Web stock adjustment followed by device reconnect → ledger and balances reconcile correctly" — no test exists; `POS.Web` has no stock-adjustment screen to begin with (checked: `ManagementController`'s inventory view is read/report-only), so this row is currently not reachable to test until that surface exists. "Migration of existing populated database → counts, IDs, posted totals and stock invariants preserved" — covered only by the SQLite rehearsal already on record from T1; the PostgreSQL side remains exactly where it was (see point 6).
6. **PostgreSQL live rehearsal: re-checked, still genuinely blocked in this environment, not assumed.** Re-verified directly this session (not carried forward from memory): no `docker` and no `psql` on PATH. Same static-only evidence as every prior session: `dotnet ef migrations script` against the **entire** migration history (including this session's new `UpgradeAdminRolesManageStores`) forced to the Npgsql provider completes without error and renders syntactically plausible PostgreSQL DDL — confirmed the new migration's bitwise `UPDATE ... SET "PermissionsMask" = "PermissionsMask" | 64 WHERE ...` renders unchanged for Postgres. This is strictly weaker than an applied migration against a live database (cannot catch extension/permission/runtime/MVCC-concurrency issues) and remains the ceiling of what this environment can verify. **What a live rehearsal needs to actually happen:** a reachable PostgreSQL instance (Docker Desktop + `docker run postgres` is the simplest path, or any externally reachable Postgres connection string) and `POS.Api`/`POS.Web`'s `Database:Provider=Postgres` + a real `ConnectionStrings:Default` pointed at it — at that point, `dotnet ef database update` against the full history, then re-running the two "empirically un-reproducible under SQLite" concurrency tests (`Concurrent_retries_of_the_same_invoice_completion_produce_exactly_one_stock_effect` and this pass's own rollback test, adapted to real multi-connection concurrency) directly against it, is the concrete next action — not a new design, just execution once the infrastructure exists.
7. **T7 preparation: exact blockers, not attempted.** T7 (`tenant.md` §7: backup/restore rehearsal, tenant-scoped export, replace-a-lost-register rehearsal, restore-one-tenant-without-overwriting-others, pilot with Ahmed's business) requires the same PostgreSQL access as point 6 (a realistic backup/restore rehearsal against SQLite would not exercise the production provider's actual tooling — `pg_dump`/`pg_restore` or a managed provider's snapshot mechanism, which SQLite has no equivalent of), plus a real customer/pilot business to rehearse against, neither of which exist in this environment. No T7 work was started or invented this pass — recorded here only to keep the gate explicit, per the same standing instruction as every prior session.
- **Tests:** 7 new this pass (`RoleUpgradeMigrationTests` × 2, `SyncConflictClassifierTests` × 2, `WebIntegrationTests.Revoking_store_access_mid_session_blocks_the_very_next_protected_request`, `ApiIntegrationTests.Failed_concurrent_push_rolls_back_invoice_inventory_and_stock_ledger_writes_together`, `CashSessionServiceTests.Closing_a_cash_session_belonging_to_a_different_tenant_and_store_is_rejected`). Full solution build clean (0 warnings/0 errors); full `POS.Tests` suite: **164/164 passing** (157 baseline + 7 new).

**Offline stock reconciliation policy, audited Web stock adjustments, isolated staging deployment, and the first live PostgreSQL rehearsal (2026-09-22, new session):** the project owner made the two product decisions that had blocked T6's last two matrix rows, then authorized isolated staging deployment on their own Docker/Traefik host to finally run the PostgreSQL rehearsal every prior session had left explicitly pending.

1. **Offline stock reconciliation policy, implemented exactly as decided:** enforce the configured negative-stock rule against each device's own local balance (already true — `SaleService` checks `AllowNegativeStock` against the selling device's own `Inventory` snapshot); on reconnection, apply every completed sale exactly once, never discard/clamp/overwrite; if the combined result goes negative, record a **visible discrepancy** instead. New `StockMovement.IsDiscrepancy` flag (set when a reconciled delta drives quantity negative) + a `StockDiscrepancyDetected` audit-log entry + a "Discrepancies only" filter and badge on `POS.Web`'s existing stock ledger. Documented in `docs/SYNC_STRATEGY.md`, including the explicit line tenant.md §6.11 calls for: strict global stock availability is **not** guaranteed while devices are offline.
2. **A real lost-update race fixed along the way, found by re-reading the actual reconciliation code rather than assumed safe:** `ReconcileInvoiceInventoryAsync` (mirrored in `POS.Api/Program.cs` and `InvoiceSyncService.cs`) applied each invoice's stock delta via an in-memory `inventory.Quantity += delta` then a deferred `SaveChangesAsync` — a classic lost-update race if two *different* invoices for the *same* product are reconciled by genuinely concurrent requests (exactly the "two offline devices, one product" scenario this task exists to test). Fixed in both mirrored copies: the delta now applies as an atomic DB-level increment (`ExecuteUpdateAsync` with `SET Quantity = Quantity + @delta`) inside the existing transaction, with the tracked entity re-synced from the DB but explicitly excluded from the batch's own later `SaveChangesAsync` (`IsModified = false`) so that call can't redundantly, non-atomically re-overwrite what the atomic statement already committed.
3. **`POS.Web`'s stock-adjustment surface: reused and completed, not rebuilt.** The existing product-edit "InitialStock" field (already the mechanism both WPF and Web used for manual stock changes) now requires a reason whenever stock changes — enforced at the `POS.Web` controller layer only, so WPF's shared write path is untouched. The write itself switched from a blind absolute overwrite to the same atomic delta-based increment as point 2, so a concurrent device reconciliation landing at the same moment is combined with the adjustment instead of lost. The existing `ManualStockAdjusted` audit entry (already recording product/old→new quantity via the existing `_session`-derived actor/store) now also records the reason.
4. **5 new tests close both previously-blocked T6 rows:** `Two_offline_devices_selling_the_last_unit_both_apply_and_the_shortfall_is_a_visible_discrepancy` and `Concurrent_pushes_of_different_invoices_for_the_same_product_combine_both_stock_effects` (API); `Web_stock_adjustment_without_a_reason_is_rejected`, `Web_stock_adjustment_with_a_reason_succeeds_and_is_audited`, `Web_stock_adjustment_followed_by_a_reconnecting_devices_sale_reconciles_both_effects` (Web). Full `POS.Tests` suite: **169/169 passing.**
5. **Isolated staging deployment stood up on the project owner's own Docker/Traefik host** (`159.195.136.212`, `/opt/sites/pos`), confirmed as a genuinely separate stack from every other tenant already running there: `pos-postgres` (own named volume, own internal network, **no host port** — never reachable outside the compose network), `pos-api`, `pos-web`, all under Compose project name `pos`. See `docs/STAGING_DEPLOYMENT.md` for exact commands and `deploy/` for the Dockerfiles/compose file/`.env.example` (no secrets committed — real secrets generated on the server with `openssl rand`, `.env` chmod 600, gitignored).
6. **The live PostgreSQL rehearsal found a real, functional-breaking bug that no prior static check had caught, and it is now fixed — full detail in a dedicated section below.** In short: the existing SQLite-scaffolded migration history, applied directly to real PostgreSQL, produced `text`/`integer` columns instead of `uuid`/`timestamptz`/`boolean`/`numeric`, and the very first real query failed (`argument of NOT must be type boolean, not type integer`). Fixed with a separate, properly-typed PostgreSQL migration history (`src/POS.Infrastructure.Migrations.Postgres`), per EF Core's own documented pattern for one `DbContext` with multiple providers. See `docs/POSTGRES_MIGRATIONS.md`.
7. **After the fix, the rehearsal was re-run for real against the live staging container and passed. Evidence is recorded separately per scenario, not as one bundled claim:**
   - **Fresh install:** a `Database:ApplyMigrationsOnStartup=true` boot against an empty PostgreSQL database started cleanly (0 errors); `POST /api/platform/tenants` provisioned a real tenant/store/admin; a real login issued a working JWT; a real category → product → paid-invoice sync push correctly decremented stock (5 → 3).
   - **Concurrent *distinct* sales (different invoices, same product):** a second overlapping device's sale applied on top (3 → -2) and was correctly flagged `IsDiscrepancy = true` with a `StockDiscrepancyDetected` audit row — the policy from point 1, now proven against real PostgreSQL. Then, separately, **10 genuinely concurrent invoice pushes** for the same product (true parallel background HTTP requests, not sequential) all applied — final quantity reflected all 10 deltas combined (−2 → −12) with **zero lost updates**. This is the authoritative concurrency confirmation every prior session deferred to "the still-pending live rehearsal" — SQLite's single-writer test host could never reproduce a true interleave; real PostgreSQL MVCC just did, and the atomic-increment fix from point 2 held.
   - **Concurrent *duplicate* pushes of the *same* sale (the lost-ack-retry scenario):** 8 truly concurrent pushes of one identical invoice payload (same invoice/line/payment IDs) produced exactly **one** `Applied` result and seven `Skipped` results, zero errors. Verified directly in the database, not just from the HTTP responses: exactly one `StockMovement` row (delta −1), exactly one `Payment` row (10.00) — a single financial and stock effect regardless of how many times the identical push was retried concurrently.
   - **Idempotent re-migration against now-populated data (a narrower, related check — not the same as point 8's still-pending item):** with the staging database now holding 13 invoices, 14 stock movements, 3 payments and real financial totals from the tests above, the API container was restarted with `ApplyMigrationsOnStartup=true` again, forcing `Database.Migrate()` to run against a populated, already-fully-migrated database. Row counts (Tenants/Stores/Products/Invoices/StockMovements/Inventories/Payments) and both financial/stock invariants (`SUM(Payments.Amount) = 80.00`, `Inventories.Quantity = -13.0000`) were identical before and after. This proves re-running the migrator against real data is safe; it does **not** prove a schema-version upgrade (N → N+1) preserves data, since no such upgrade exists yet — see point 8.
8. **What the rehearsal explicitly could not cover yet — kept pending, not claimed as done:** a genuine "populated pre-upgrade PostgreSQL database, migrated forward across a schema change" test does not yet apply — `InitialPostgresSchema` is the *first* PostgreSQL migration this project has ever had, so there is no earlier PostgreSQL schema version to migrate *from*. This row becomes testable the next time a schema change adds a second PostgreSQL migration (see `docs/POSTGRES_MIGRATIONS.md` for the now-required "every schema change needs two migrations" process) — at that point, the same populate-then-migrate-then-verify-invariants method already proven for SQLite (`RoleUpgradeMigrationTests`) should be repeated against PostgreSQL specifically. Backup/restore rehearsal (T7 scope) was not attempted. Test data left in staging deliberately (the `rehearsal-co` tenant, its product, and the invoices pushed above) as a working example rather than cleaned up — treat it as disposable staging fixture data, not anything to preserve.
9. **External HTTPS access: fixed and verified. No longer a blocker.** Root cause: the wildcard `*.sparkco.vip` DNS is Cloudflare-proxied and the shared Traefik instance's only configured ACME method is `tlsChallenge` (TLS-ALPN-01), which cannot work through a TLS-terminating proxy — confirmed directly from Traefik's own logs (`Cannot negotiate ALPN protocol "acme-tls/1"`). Firewall reachability was separately confirmed clean beforehand (no blocker) via a direct `curl --resolve` to the server's IP on port 443, bypassing Cloudflare. The project owner added two non-proxied ("DNS only") A records — `pos-staging.sparkco.vip` and `pos-api-staging.sparkco.vip`, both to `159.195.136.212` — leaving the wildcard and shared Traefik config completely untouched. Once propagated, Traefik's existing `tlsChallenge` resolver obtained real certificates automatically, with no server-side changes needed. **Verified externally, from outside the server, with real trusted certificates (no `-k`/insecure flag needed):** `https://pos-api-staging.sparkco.vip/health` → `{"status":"ok","database":"reachable"}`; `/api/meta/ping` confirms `databaseProvider: Postgres`; `https://pos-staging.sparkco.vip/` correctly requires the configured basic-auth credentials (401 without, a normal app redirect with). Both application-level authentication (JWT login, cookie auth) and the staging-specific access control (Traefik basic auth on the dashboard) are intact and working together correctly.
10. **T6 matrix, closed for real:** both previously-blocked rows ("concurrent offline last-unit sales" and "web stock adjustment followed by device reconnect") now have policy, implementation, and test coverage on both SQLite (this repo's full suite) and, for the reconciliation/concurrency mechanism specifically, live PostgreSQL. Every other T6 row already recorded as covered in prior dated entries is unchanged.

**Windows client (WPF) staging rehearsal, part 1: cash session, offline sales, and printing (2026-09-24/25, later session) — found and fixed 5 real bugs, none of them in the actual sync/reconciliation business logic:**

The project owner walked through the manual runbook (`docs/STAGING_WPF_REHEARSAL.md`) live, on the same Windows machine this session runs on, with each step guided in real time. This is the first time this project's actual compiled WPF client has ever been run against a live deployment and driven by a human — everything found below was structurally invisible to the automated test suite, which exercises the same underlying service methods directly and bypasses the WPF host/GUI layer entirely.

1. **Cash session, offline sales, refund, and logout-survives-session all worked correctly on the first try** — no bugs found in that part of the flow. Confirmed: opening float, running summary, offline sale/refund recording while `Sync:Enabled=false`, and a real logout leaving the cash session open across a re-login.
2. **Receipt printing blocked sale completion for several seconds — a real, reproducible bug, fixed.** `MainViewModel.CompleteSaleAsync` called `_printer.Print(result.Receipt)` synchronously on the UI thread; `EscPosReceiptPrinter` talks to the Windows print spooler for whatever is currently the OS default printer, which can hang for several seconds if that printer is unreachable (a real, machine-specific condition on the rehearsal laptop). Fixed by running the print call on a background `Task.Run`, never awaited, so it can't delay the success message regardless of printer state. A follow-up (a proper "which printer" selection setting, instead of trusting the ambiguous OS-wide default) was deliberately not built now — added to ROADMAP.md's Operational Extensions backlog per the project owner's explicit instruction.
3. **`SetupWindow`'s "join an existing business" submit button was clipped off the visible window** — a fixed `Height="480"`/`ResizeMode="NoResize"` was sized for the shorter "choice" panel, not the taller join form (4 fields + password + buttons). Fixed with `SizeToContent="Height"` plus a `MinHeight` floor and resize-as-fallback, so the window always fits whichever panel is showing.
4. **Background sync had, as far as this project's history can tell, never actually run in the real compiled app — a fundamental host-lifecycle bug, fixed.** `App.xaml.cs` built the Generic Host (`Host.CreateDefaultBuilder().Build()`) but never called `_host.Start()`. `IHostedService.StartAsync` — what actually runs `InvoiceSyncBackgroundService.ExecuteAsync` — is only invoked once the host itself starts; building it alone only constructs the DI container. Confirmed via a raw-file diagnostic trace (bypassing Serilog, which turned out to have its own separate, still-unresolved silent-failure issue in this environment — see point 6) that after the fix, `ExecuteAsync` genuinely starts firing on its interval.
5. **Even after that fix, sync ran every interval with no error and pushed nothing — a second, independent bug: `TryCreateAuthorizedClientAsync`'s own re-login never sent a tenant slug.** The API can only auto-resolve an omitted slug when exactly one active tenant exists system-wide (`AuthService.ResolveTenantIdAsync`) — true for a lone bootstrap tenant, false the moment any second tenant is ever provisioned, which is staging's actual condition (the bootstrap tenant plus `rehearsal-co`). The slug is known once, at "join an existing business" time, but was never persisted for this later reuse. Fixed: `BusinessJoinService` now persists it as a new `Sync.TenantSlug` store-scoped setting, and `TryCreateAuthorizedClientAsync` reads it back on every re-login. (This device had joined before the fix existed, so its own setting was backfilled directly for this rehearsal — every future join persists it automatically.)
6. **Even after both of those fixes, login succeeded but every device/register/invoice push still failed (500/422) — a third, deeper, genuinely significant bug: PostgreSQL/Npgsql rejects `DateTime` values with `Kind=Unspecified`, which is exactly what SQLite always hands back.** Confirmed directly from the API's own container logs: `System.ArgumentException: Cannot write DateTime with Kind=Unspecified to PostgreSQL type 'timestamp with time zone', only UTC is supported.` Every prior PostgreSQL rehearsal check in this project's history had used hand-built JSON payloads with explicit `"Z"`-suffixed timestamps (which deserialize as `Kind=Utc`), so this was never actually exercised until a real SQLite-backed client — whose own local timestamps are `Kind=Unspecified` by construction — pushed real data. Fixed at the source, not per call site: `PosDbContext.ConfigureConventions` now applies a global EF value-conversion convention forcing every `DateTime`/`DateTime?` property to `Kind=Utc` on both read and write. Verified as a pure runtime behavior change, not a schema change, for both providers (`dotnet ef migrations has-pending-model-changes` — no changes, SQLite and Postgres both). A no-op for SQLite, which ignores `DateTimeKind` entirely, so the existing desktop client and the full SQLite-based test suite are unaffected.
7. **Verified end-to-end after all three sync fixes, against the live staging deployment, not assumed:** the same real WPF client — login succeeds with the correct tenant, the device and its register both push successfully (`count=14`, `OK`), and all 13 locally-queued invoices apply (`push HTTP 200 ... :Applied` for every one). This is the first time in this project's history that its actual compiled WPF client has been confirmed to sync with a real server at all.
8. **A separate, still-open finding, not fixed this session:** Serilog's file sink (`App.xaml.cs`'s `UseSerilog` configuration) silently wrote nothing at all during this rehearsal, on this machine, for reasons not diagnosed (a raw `File.AppendAllText` bypass was used instead to get real diagnostic output). Worth investigating before relying on the desktop app's own log files for any future field diagnosis — recorded here so it isn't lost, not chased down mid-rehearsal to keep the actual sync bugs the priority.
9. **Full `POS.Tests` suite after all fixes: 168/169** — the one failure (`Management_get_stock_ledger_filter_supports_yesterday_preset`) is a pre-existing, unrelated bug in the test itself (mixes `DateTime.UtcNow.Date` and `DateOnly.FromDateTime(DateTime.Now)` for the same "yesterday" boundary, so it only fails near local midnight on a UTC+3 machine) — not touched, not in scope for this pass.
10. **Rehearsal status: cash session, offline sales/refund, logout, and device-based sync/reconnection are now all confirmed working manually against live staging.** Remaining runbook steps (verifying the synced data on the Web dashboard, and the closing-reconciliation walkthrough) were not yet completed as of this entry — see the next dated entry or `docs/STAGING_WPF_REHEARSAL.md`'s own "Results" section for their outcome once run.

**Stage 4T complete:** [ ]

---

## Stage 5 — Web dashboard & beyond

| Step | Status |
|------|--------|
| 5.1 Web dashboard (reports, users, inventory) | [~] | `POS.Web` MVC host added with cookie auth, Admin/Manager access policy, a reports dashboard, editable store/user/category management for profile, operational settings, currency policy, store-scoped users, and product/inventory administration, plus focused web integration coverage for login, dashboard access, and management postbacks |
| 5.2 Multi-branch reporting / policies | [ ] |
| 5.3 Mobile / PWA / MAUI pilot | [ ] |
| 5.4 Integrations / plugins (as needed) | [ ] |

**Stage 5 complete:** [ ]

---

## Summary

| Stage | Name | Complete |
|-------|------|----------|
| 0 | Foundation | Yes |
| 1 | MVP | Yes |
| 2 | Operations & UX | Yes |
| 3 | Commercial depth | Yes |
| 4 | API & sync | Yes |
| 4T | Multi-tenant SaaS foundation | No |
| 5 | Web & beyond | No |

**Reference note:** The external reference project reviewed during planning was moved out of this repo to `C:\projects\pos_q`. If we need another comparison pass later, use that location instead of expecting `pos_q/` under this repository root.

*Last updated 2026-09-24/25 (later session): the manual WPF staging rehearsal is underway, live, with the project owner — and found four more real bugs beyond the printer issue, the most significant being that background sync had, as far as this project's history can tell, never actually run in the real compiled WPF app at all (the Generic Host was built but never started), plus a missing tenant-slug on sync's own re-login, and a PostgreSQL/Npgsql DateTime-Kind rejection that crashed every device/register/invoice push once the first two were fixed. All four are fixed and verified end-to-end against live staging: the real WPF app now logs in, pushes its device/register, and applies all queued invoices. See the dated "Windows client (WPF) staging rehearsal, part 1" entry above for the full account, including one still-open, non-blocking finding (Serilog's file sink silently not writing on this machine) and what remains of the runbook (Web-dashboard verification, closing reconciliation). Full `POS.Tests` suite: **168/169** (the one failure is a pre-existing, unrelated local-time/UTC mismatch in a date-preset test, not caused by this session's changes — see the dated entry). Prior sessions (2026-09-23, 2026-09-22): external HTTPS fixed and verified; the three separated PostgreSQL evidence items (concurrent distinct sales, concurrent duplicate pushes, idempotent re-migration) each independently verified; both remaining T6 rows closed; the isolated staging deployment stood up and its first live PostgreSQL rehearsal run, finding and fixing the migration-history type-mismatch bug. Prior history: Stages 0–4 complete; Stage 4T's T0–T5 substantially landed across several earlier sessions — see this file's dated entries above for the full trail.*



